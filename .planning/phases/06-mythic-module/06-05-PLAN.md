---
phase: 06-mythic-module
plan: 05
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - api/src/services/bootstrap.ts
  - api/src/index.ts
  - api/src/openapi.yml
  - api/src/openapi.yml.test.ts
  - api/src/services/mythic-module.integration.test.ts
autonomous: true
requirements:
  - MY-04
  - MY-05
must_haves:
  truths:
    - "MythicModuleService is registered in the dependency injection/bootstrap layer"
    - "API spec documents mythic metadata fields in ReasonResponse"
    - "Integration test verifies end-to-end: identity registration → mythic generation → mythified output"
    - "Default neutral identity preserves backward compatibility for existing API consumers"
  artifacts:
    - path: "api/src/services/bootstrap.ts"
      provides: "DI registration for MythicModuleService, SymbolicAnchorLoader, and wiring"
      contains: ["MythicModuleService", "SymbolicAnchorLoader"]
    - path: "api/src/index.ts"
      provides: "Updated Express bootstrap to pass mythicModule to Orchestrator and AgentBuilder"
      contains: ["mythicModule"]
    - path: "api/src/openapi.yml"
      provides: "Updated OpenAPI spec with MythicIdentity and mythicMetadata schemas"
      contains: ["MythicIdentity", "mythicMetadata"]
    - path: "api/src/services/mythic-module.integration.test.ts"
      provides: "Integration test for full mythic pipeline"
      contains: ["describe('Mythic Module Integration'"]
  key_links:
    - from: "api/src/index.ts"
      to: "MythicModuleService"
      via: "constructor injection into Orchestrator and AgentBuilder"
      pattern: "new MythicModuleService"
    - from: "api/src/openapi.yml"
      to: "api/src/controllers/reason.ts"
      via: "schema documents ReasonResponse.mythicMetadata field"
      pattern: "mythicMetadata"
---

<objective>
Wire the Mythic Module into the application's bootstrap layer, update API documentation, and verify the entire pipeline with an end-to-end integration test.

Purpose: Plans 01-04 created the types, loader, service, and integration points. This plan makes the Mythic Module a first-class citizen of the application by registering it in DI, documenting it in OpenAPI, and proving the full pipeline works from HTTP request to mythified response.
Output: Updated bootstrap, OpenAPI spec, and integration tests demonstrating the complete mythic pipeline.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@api/src/index.ts
@api/src/services/bootstrap.ts
@api/src/openapi.yml
@api/src/controllers/reason.ts
</context>

<interfaces>
From api/src/index.ts (typical Express bootstrap):
```typescript
// Typical pattern from prior phases:
const identityService = new IdentityService();
const identityBinding = new IdentityBindingService(identityService);
const orchestrator = new Orchestrator(memoryService, providerRegistry, identityBinding);
const agentBuilder = new AgentBuilder(orchestrator, mcpClient, config);
```

From api/src/services/bootstrap.ts (if exists, otherwise DI is inline in index.ts):
```typescript
// Service registration and wiring
```

From api/src/openapi.yml (current):
```yaml
components:
  schemas:
    ReasonResponse:
      type: object
      properties:
        id: { type: string }
        output: { type: string }
        status: { type: string }
        identity_anchor: { type: string }
        reasoning: { type: object }
        metadata: { type: object }
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: register MythicModuleService in bootstrap/DI layer</name>
  <files>api/src/services/bootstrap.ts, api/src/index.ts</files>
  <action>
1. Check if `api/src/services/bootstrap.ts` exists:
   - If it exists: update it to register MythicModuleService and SymbolicAnchorLoader
   - If it doesn't exist: update `api/src/index.ts` directly (DI is likely inline there)

2. Registration order (must respect dependency chain):
   ```typescript
   // After identityBinding is created
   const anchorLoader = new SymbolicAnchorLoader();
   const mythicModule = new MythicModuleService(identityBinding, anchorLoader);
   
   // Pass to Orchestrator
   const orchestrator = new Orchestrator(
     memoryService,
     providerRegistry,
     identityBinding,
     mythicModule  // 4th parameter
   );
   
   // Pass to AgentBuilder
   const agentBuilder = new AgentBuilder(
     orchestrator,
     mcpClient,
     { enableTools: true, maxToolIterations: 5, mythicModule }
   );
   
   // Pass to MultiAgentOrchestrator if it exists
   const multiAgentOrchestrator = new MultiAgentOrchestrator(
     orchestrator,
     agentBuilder,
     mythicModule  // if constructor accepts it
   );
   ```

3. Verify all services initialize without errors:
   - SymbolicAnchorLoader should load v1.json successfully
   - MythicModuleService should accept the dependencies
   - No circular dependencies introduced

4. Add startup log:
   - `console.log('[Bootstrap] Mythic Module registered with ' + anchorLoader.loadAnchors().length + ' symbolic anchors')`
  </action>
  <verify>
    <automated>npm run build && node -e "console.log('Bootstrap compiles successfully')"</automated>
  </verify>
  <done>MythicModuleService and SymbolicAnchorLoader are instantiated and wired into Orchestrator, AgentBuilder, and MultiAgentOrchestrator; application compiles</done>
</task>

<task type="auto">
  <name>task 2: update OpenAPI spec with mythic schemas</name>
  <files>api/src/openapi.yml</files>
  <action>
Update `api/src/openapi.yml`:

1. Add `MythicIdentity` schema under `components.schemas`:
   ```yaml
   MythicIdentity:
     type: object
     properties:
       identityId: { type: string, description: "Reference to SigilIdentity.id" }
       tone:
         type: object
         properties:
           formality: { type: string, enum: [casual, neutral, formal, ceremonial] }
           emotionalRange: { type: string, enum: [stoic, measured, expressive, passionate] }
           directness: { type: string, enum: [indirect, balanced, direct, blunt] }
           humor: { type: string, enum: [none, dry, witty, playful] }
           vocabulary: { type: string, enum: [simple, standard, technical, arcane] }
       voice:
         type: object
         properties:
           perspective: { type: string, enum: [first, second, third] }
           sentenceComplexity: { type: string, enum: [simple, compound, complex] }
           pacing: { type: string, enum: [rapid, steady, deliberate, ritualistic] }
           rhythmPattern: { type: string }
           signaturePhrases: { type: array, items: { type: string } }
       anchors:
         type: array
         items:
           type: object
           properties:
             type: { type: string, enum: [geometry, color, ratio, archetype, motion] }
             name: { type: string }
             value: { type: object }
             weight: { type: number }
             description: { type: string }
       constraints:
         type: array
         items:
           type: object
           properties:
             constraint: { type: string }
             scope: { type: string, enum: [opening, closing, throughout, transition] }
             severity: { type: string, enum: [suggestion, requirement, imperative] }
       sigilHash: { type: string }
       version: { type: integer }
       generatedAt: { type: string, format: date-time }
   ```

2. Update `ReasonResponse` schema:
   - Add `mythicMetadata` property (optional):
     ```yaml
     mythicMetadata:
       type: object
       properties:
         identityId: { type: string }
         tone:
           type: object
           properties:
             formality: { type: string }
             emotionalRange: { type: string }
             directness: { type: string }
             humor: { type: string }
             vocabulary: { type: string }
         voice:
           type: object
           properties:
             perspective: { type: string }
             sentenceComplexity: { type: string }
             pacing: { type: string }
         anchorCount: { type: integer }
         constraintCount: { type: integer }
         sigilHash: { type: string }
     ```

3. Verify OpenAPI spec is valid YAML:
   - No indentation errors
   - All references resolve
   - Schema is backward compatible (only added optional fields)
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=openapi</automated>
  </verify>
  <done>OpenAPI spec includes MythicIdentity schema and ReasonResponse.mythicMetadata; spec is valid YAML; tests pass</done>
</task>

<task type="auto">
  <name>task 3: create end-to-end integration test for mythic pipeline</name>
  <files>api/src/services/mythic-module.integration.test.ts</files>
  <action>
Create `api/src/services/mythic-module.integration.test.ts` with:

1. Setup:
   - Use real IdentityService with an in-memory MongoDB instance (or mock if integration test infrastructure requires)
   - Create a test SigilIdentity with `config.mythic` pre-populated
   - Instantiate SymbolicAnchorLoader with real v1.json path
   - Instantiate MythicModuleService with real dependencies
   - Instantiate Orchestrator with mythicModule
   - Instantiate AgentBuilder with mythicModule

2. Test: "full pipeline with custom mythic identity" (skipping actual LLM call, mocking provider):
   - Register a SigilIdentity with:
     - name: "The Arcane Scholar"
     - config.mythic.tone.vocabulary: "arcane"
     - config.mythic.tone.formality: "ceremonial"
     - config.mythic.voice.perspective: "first"
   - Submit a ReasonRequest with identity_anchor set to the identity's id
   - Mock provider.generate to return: `{ text: "It is a simple fact that 2+2 equals 4." }`
   - Verify the response output:
     - Contains ritualistic opening (ceremonial tone)
     - Uses arcane vocabulary (elevated language)
     - Uses first-person perspective ("I know" instead of "It is")
   - Verify `mythicMetadata` in response:
     - identityId matches the registered identity
     - anchorCount > 0
     - sigilHash is present and non-empty

3. Test: "full pipeline with neutral identity" (backward compatibility):
   - Submit a ReasonRequest with identity_anchor: "neutral"
   - Mock provider.generate to return: `{ text: "It is a simple fact." }`
   - Verify the response output is UNCHANGED (neutral identity does not transform)
   - Verify `mythicMetadata` is either absent or shows identityId: "neutral"
   - Verify no errors occur

4. Test: "mythic identity generation from config-less identity" (fallback generation):
   - Register a SigilIdentity WITHOUT config.mythic
   - Submit a ReasonRequest with that identity
   - Verify MythicModuleService generates a mythic schema on-the-fly
   - Verify the generated output reflects the default tone/voice

5. Test: "symbolic anchors are bound to output" (MY-05):
   - Verify that when the identity has ratio anchors (e.g., golden), the output references the symbolic concept
   - Verify anchorCount in mythicMetadata matches the number of loaded anchors

6. Cleanup:
   - Clear any in-memory databases or caches
   - Reset mock providers
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=mythic-module.integration.test.ts</automated>
  </verify>
  <done>Integration test verifies end-to-end mythic pipeline with custom identity, neutral identity, fallback generation, and symbolic anchor binding; all 5 scenarios pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bootstrap → Services | Service instantiation crosses here |
| OpenAPI spec → API consumers | Contract boundary |
| Integration test → Real services | Tests exercise real file system and (mocked) LLM calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Denial of Service | SymbolicAnchorLoader on startup | mitigate | Try/catch around loader initialization; app starts even if v1.json is missing |
| T-06-18 | Information Disclosure | OpenAPI spec reveals internal schemas | accept | MythicIdentity schema contains no sensitive data; all fields are user-defined |
| T-06-19 | Tampering | Integration test mocks | accept | Tests use isolated mocks; no production impact |
| T-06-20 | Repudiation | Integration test coverage | accept | Tests verify behavior but are not an audit trail (Phase 8 handles audit) |
</threat_model>

<verification>
- `npm run build` in `api/` directory succeeds
- `npm test -- --testPathPattern=mythic-module.integration` passes (5 scenarios)
- OpenAPI YAML is valid (can be parsed by Swagger UI or yaml linter)
- Application bootstrap logs "Mythic Module registered with N symbolic anchors"
- Backward compatibility: existing API consumers without mythic awareness still work
</verification>

<success_criteria>
- MythicModuleService is registered in bootstrap and instantiated on startup
- Orchestrator and AgentBuilder receive mythicModule in constructor
- OpenAPI spec documents MythicIdentity and ReasonResponse.mythicMetadata schemas
- Integration test passes for custom identity, neutral identity, fallback generation, and symbolic anchors
- Application compiles and all existing tests pass
- No breaking changes to existing API contracts (all new fields optional)
</success_criteria>

<output>
After completion, create `.planning/phases/06-mythic-module/06-05-SUMMARY.md`
</output>
