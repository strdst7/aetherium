---
phase: 12-miii-aim-brand-melody
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - design/sigil/v1.json
  - api/src/services/mythic-module.ts
  - api/src/adapters/gemini-provider.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-06]
user_setup: []

must_haves:
  truths:
    - "Aetherium design tokens include MIII-AIM gold, obsidian, silver, and violet palettes"
    - "Mythic module accepts sovereign and architectural voice/tone presets with text transformations"
    - "Gemini provider defaults to gemini-2.5-flash and gemini-embedding-2"
    - "Design token compliance rules enforce golden ratio (1.618:1) and vault proportions (3:1)"
  artifacts:
    - path: "design/sigil/v1.json"
      provides: "MIII-AIM color palettes + compliance rules"
      min_lines: 230
      contains: "secondary"
    - path: "api/src/services/mythic-module.ts"
      provides: "sovereign + architectural mythic presets"
      exports: ["makeSovereign", "makeArchitectural"]
    - path: "api/src/adapters/gemini-provider.ts"
      provides: "Gemini model defaults"
      contains: "gemini-2.5-flash"
  key_links:
    - from: "api/src/services/mythic-module.ts"
      to: "design/sigil/v1.json"
      via: "SymbolicAnchorLoader parsing golden ratio / vault proportion constraints"
      pattern: "SymbolicAnchorLoader|v1.json"
    - from: "api/src/adapters/gemini-provider.ts"
      to: "GEMINI_MODEL env var"
      via: "process.env.GEMINI_MODEL fallback to gemini-2.5-flash"
      pattern: "process\\.env\\.GEMINI_MODEL"

---

<objective>
Verify and confirm the MIII-AIM brand foundation is correctly applied across design tokens, mythic voice presets, and Gemini model defaults. All implementation work is already complete per the discuss-phase session — this plan provides formal verification.

Purpose: Ensure the Aetherium design system carries MIII-AIM secondary palettes, the mythic module supports sovereign/architectural voice modes, and the Gemini provider uses up-to-date model defaults.
Output: Verified design tokens, verified mythic presets, verified Gemini defaults.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-miii-aim-brand-melody/12-CONTEXT.md
@.planning/phases/12-miii-aim-brand-melody/12-UAT.md
@.planning/phases/12-miii-aim-brand-melody/12-UI-SPEC.md

<interfaces>
<!-- Key types used in mythic module -->
From api/src/types/mythic.ts:
```typescript
export interface ToneModel {
  register: 'neutral' | 'ceremonial' | 'sovereign' | 'oracular' | 'alchemical';
  intensity: number; // 0.0 to 1.0
  modifiers: string[];
}
export interface VoiceModel {
  character: 'neutral' | 'architectural' | 'oracular' | 'elemental';
  vocabulary: string;
  sentenceStructure: string;
  pacing: string;
}
```

From design/sigil/v1.json (token structure):
```json
{
  "colors": {
    "secondary": { "300": "#D9C27A", ... },
    "obsidian": { "800": "#0A0A0A", ... },
    "silver": { "300": "#C9D1D9", ... },
    "violet": { "600": "#3A1F5D", ... }
  },
  "ratios": { "golden": "1.618:1", "vault": "3:1" },
  "compliance": { "rules": [...] }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Verify MIII-AIM design token palettes and compliance rules</name>
  <files>design/sigil/v1.json</files>
  <read_first>design/sigil/v1.json</read_first>
  <action>
    Verify that `design/sigil/v1.json` contains the MIII-AIM brand palettes (all already implemented per discuss-phase). Run the following checks:
    
    1. **Secondary (gold) palette** — Must have `colors.secondary` object with at minimum `"300": "#D9C27A"` and 9 other shades (100-900).
    2. **Obsidian palette** — Must have `colors.obsidian` object with at minimum `"800": "#0A0A0A"` and 9 other shades.
    3. **Silver palette** — Must have `colors.silver` object with at minimum `"300": "#C9D1D9"` and 9 other shades.
    4. **Violet palette** — Must have `colors.violet` object with at minimum `"600": "#3A1F5D"` and 9 other shades.
    5. **Compliance rules** — Must include a rule referencing MIII-AIM golden ratio `1.618:1` for identity artifacts with obsidian/gold at 8:1, and a vault rule with violet-to-gold ratio `3:1` with obsidian base layer.

    If any check fails, report the gap as a verification failure. Do NOT modify the file.
  </action>
  <acceptance_criteria>
    - `design/sigil/v1.json` contains `"secondary"` key with `"300": "#D9C27A"`
    - `design/sigil/v1.json` contains `"obsidian"` key with `"800": "#0A0A0A"`  
    - `design/sigil/v1.json` contains `"silver"` key with `"300": "#C9D1D9"`
    - `design/sigil/v1.json` contains `"violet"` key with `"600": "#3A1F5D"`
    - `design/sigil/v1.json` contains `"golden"` ratio at `"1.618:1"`
    - `design/sigil/v1.json` contains `"violet-to-gold ratio 3:1"`
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"secondary"' design/sigil/v1.json && grep -q '"obsidian"' design/sigil/v1.json && grep -q '"silver"' design/sigil/v1.json && grep -q '"violet"' design/sigil/v1.json && grep -q '1.618.*1' design/sigil/v1.json && grep -q '3:1' design/sigil/v1.json && echo "ALL PALETTES VERIFIED" || echo "MISSING PALETTES"</automated>
  </verify>
  <done>
    All four MIII-AIM color palettes (secondary/gold, obsidian, silver, violet) present in v1.json with compliance rules for golden ratio and vault proportions.
  </done>
</task>

<task type="auto">
  <name>task 2: Verify sovereign and architectural mythic voice presets</name>
  <files>api/src/services/mythic-module.ts</files>
  <read_first>api/src/services/mythic-module.ts</read_first>
  <action>
    Verify that `api/src/services/mythic-module.ts` contains the sovereign and architectural voice presets (all already implemented per discuss-phase). Run the following checks:

    1. **Sovereign tone detection** — `mythify()` method must check for `register === "sovereign"` and call `this.makeSovereign(output)`.
    2. **Architectural voice detection** — `mythify()` method must check for `character === "architectural"` and call `this.makeArchitectural(output)`.
    3. **Sovereign auto-detection** — When `customRules` contain "sovereign" or "architectural" keywords and register is "neutral", the register must auto-switch to "sovereign" with modifiers `["architectural", "ritualistic"]`.
    4. **Architectural auto-detection** — When rules contain "architectural" or "sovereign", character must auto-switch to "architectural".
    5. **makeSovereign() method** — Must replace tentative language ("I think" → "It is determined that"), elevate action verbs ("use" → "invoke", "make" → "forge"), and append `[⊹ Stamped by Sovereign Accord — sigil bound]`
    6. **makeArchitectural() method** — Must use Roman numeral enumeration ("first" → "I."), replace "because" with "per", "example" with "exemplar", "part" with "module", "group" with "cluster".

    If any check fails, report the gap. Do NOT modify the file.
  </action>
  <acceptance_criteria>
    - `mythic-module.ts` contains `register === "sovereign"` guard calling `makeSovereign`
    - `mythic-module.ts` contains `character === "architectural"` guard calling `makeArchitectural`
    - `mythic-module.ts` contains `"I think"` replacement → `"It is determined that"` in `makeSovereign`
    - `mythic-module.ts` contains `Stamped by Sovereign Accord` stamp in `makeSovereign`
    - `mythic-module.ts` contains `"because"` → `"per"` replacement in `makeArchitectural`
    - `mythic-module.ts` contains `"first"` → `"I."` replacement in `makeArchitectural`
    - `mythic-module.ts` contains auto-detect logic: `"sovereign"` or `"architectural"` in rules triggers preset activation
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'register === "sovereign"' api/src/services/mythic-module.ts && grep -q 'character === "architectural"' api/src/services/mythic-module.ts && grep -q 'It is determined that' api/src/services/mythic-module.ts && grep -q 'Stamped by Sovereign Accord' api/src/services/mythic-module.ts && grep -q '"because", "per"' api/src/services/mythic-module.ts && echo "MYTHIC PRESETS VERIFIED" || echo "MISSING MYTHIC PRESETS"</automated>
  </verify>
  <done>
    MythicModule has sovereign tone preset (register detection + makeSovereign transformation + Sovereign Accord stamp) and architectural voice preset (character detection + makeArchitectural transformation with Roman numeral enumeration and domain vocabulary).
  </done>
</task>

<task type="auto">
  <name>task 3: Verify Gemini model defaults are updated (Spike 001 cleanup)</name>
  <files>api/src/adapters/gemini-provider.ts</files>
  <read_first>api/src/adapters/gemini-provider.ts</read_first>
  <action>
    Verify that `api/src/adapters/gemini-provider.ts` uses updated model defaults (per D-03, already implemented). Run the following checks:

    1. **Constructor model default** — Must default to `"gemini-2.5-flash"` (not `"gemini-1.5-pro"`). The pattern should be `process.env.GEMINI_MODEL || "gemini-2.5-flash"`.
    2. **Embedding URL** — The embedding API URL must reference `gemini-embedding-2` (not `text-embedding-004`). Pattern: `models/gemini-embedding-2:embedContent`.

    If any check fails, apply the fix:
    - Replace `gemini-1.5-pro` with `gemini-2.5-flash` in the constructor default (line ~21)
    - Replace `text-embedding-004` with `gemini-embedding-2` in the embed URL (line ~170)
  </action>
  <acceptance_criteria>
    - `gemini-provider.ts` contains `"gemini-2.5-flash"` as default model (not `"gemini-1.5-pro"`)
    - `gemini-provider.ts` embed URL uses `gemini-embedding-2` (not `text-embedding-004`)
    - Pattern follows existing convention: `process.env.GEMINI_MODEL || "gemini-2.5-flash"`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'gemini-2.5-flash' api/src/adapters/gemini-provider.ts && grep -q 'gemini-embedding-2' api/src/adapters/gemini-provider.ts && ! grep -q 'gemini-1.5-pro' api/src/adapters/gemini-provider.ts && ! grep -q 'text-embedding-004' api/src/adapters/gemini-provider.ts && echo "GEMINI DEFAULTS VERIFIED" || echo "GEMINI DEFAULTS NEED FIX"</automated>
  </verify>
  <done>
    GeminiProvider defaults to gemini-2.5-flash (model) and gemini-embedding-2 (embeddings). No references to gemini-1.5-pro or text-embedding-004 remain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — Phase 12 is a brand/presentation layer change only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T | design/sigil/v1.json | accept | Best-effort brand integrity check — no validated runtime enforcement |
</threat_model>

<verification>
- [x] Task 1: All four MIII-AIM palettes in v1.json with compliance rules (grep command passes)
- [x] Task 2: Mythic sovereign/architectural presets with text transformations (grep command passes)
- [x] Task 3: Gemini model defaults at gemini-2.5-flash / gemini-embedding-2 (grep command passes)
- [ ] Task 3: No remaining references to gemini-1.5-pro or text-embedding-004
</verification>

<success_criteria>
- design/sigil/v1.json has MIII-AIM secondary, obsidian, silver, violet palettes with compliance rules
- api/src/services/mythic-module.ts has makeSovereign() and makeArchitectural() transformations
- api/src/adapters/gemini-provider.ts defaults to gemini-2.5-flash model and gemini-embedding-2 URL
- grep-based verification commands all return positive confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/12-miii-aim-brand-melody/12-01-SUMMARY.md`
</output>
