---
phase: 12-miii-aim-brand-melody
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/components/Layout.tsx
  - web/components/Button.tsx
  - web/pages/index.tsx
autonomous: true
requirements: [D-01, D-02, D-05, D-06]
user_setup: []

must_haves:
  truths:
    - "Header displays '⚡ Aetherium × MIII-AIM' with gold separator"
    - "Footer displays '⊹ Powered by MIII-AIM Engine' with gold decorative glyph"
    - "Landing page shows architecture footnote with MIII-AIM attribution"
    - "Button component accepts variant='gold' with gold background (#D9C27A) and obsidian text (#0A0A0A)"
  artifacts:
    - path: "web/components/Layout.tsx"
      provides: "Shared layout with MIII-AIM branded header and footer"
      contains: "MIII-AIM"
    - path: "web/components/Button.tsx"
      provides: "Button with gold variant"
      exports: ["gold variant"]
    - path: "web/pages/index.tsx"
      provides: "Landing page with MIII-AIM architecture footnote"
      contains: "MIII-AIM Sovereign Engine"
  key_links:
    - from: "web/components/Layout.tsx"
      to: "web/pages/index.tsx"
      via: "Layout wraps landing page content"
      pattern: "Layout"
    - from: "web/components/Button.tsx"
      to: "web/pages/index.tsx"
      via: "Button used in landing page call-to-action"
      pattern: "Button"

---

<objective>
Verify the MIII-AIM UI brand elements are correctly applied across the shared layout, button component, and landing page. All implementation work is already complete per the discuss-phase session — this plan provides formal verification.

Purpose: Ensure the Aetherium web UI carries the MIII-AIM brand "shade/hint" across header, footer, landing page footnote, and the gold button variant, matching the UI-SPEC.md design contract exactly.
Output: Verified header branding, footer attribution, landing page footnote, and gold button variant.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-miii-aim-brand-melody/12-CONTEXT.md
@.planning/phases/12-miii-aim-brand-melody/12-UI-SPEC.md
@.planning/phases/12-miii-aim-brand-melody/12-UAT.md

<interfaces>
<!-- Component props interfaces used in web -->
From web/components/Button.tsx (expected interface):
```typescript
interface ButtonProps {
  variant?: 'primary' | 'gold';
  children: React.ReactNode;
  onClick?: () => void;
  disabled?: boolean;
  style?: React.CSSProperties;
}
```

From web/components/Layout.tsx (expected structure):
```typescript
interface LayoutProps {
  children: React.ReactNode;
}
// Layout renders: header (brand nav) -> main (children) -> footer
```

Copywriting contract from UI-SPEC.md:
```
| Element | Copy |
|---------|------|
| Header brand | ⚡ Aetherium × MIII-AIM |
| Footer | ⊹ Powered by MIII-AIM Engine |
| Landing page footnote | ⊹ Architecture: MIII-AIM Sovereign Engine v1.0 · Identity-routed via Aetherium Crystal Core |
| Button variant label | gold (programmatic prop, not display text) |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Verify header and footer MIII-AIM branding in Layout</name>
  <files>web/components/Layout.tsx</files>
  <read_first>web/components/Layout.tsx</read_first>
  <action>
    Verify that `web/components/Layout.tsx` contains MIII-AIM header and footer branding (already implemented per discuss-phase). Run the following checks per UI-SPEC.md:

    1. **Header brand** — Must render `⚡ Aetherium` unchanged, followed by a gold (`#D9C27A`) `×` separator (`fontSize: '1.2em', fontWeight: 300`), followed by `MIII-AIM` in muted text (`#6b7280`, `fontWeight: 500`, `letterSpacing: '0.02em'`).
    2. **Footer attribution** — Must render a centered footer with `⊹` gold decorative glyph (`#D9C27A`) followed by `Powered by MIII-AIM Engine` in muted text (`#9ca3af`, `0.8em`). Separated from main content by `1px solid #e5e7eb` top border.

    If any check fails, apply the fix using inline React.CSSProperties following the existing codebase pattern.
  </action>
  <acceptance_criteria>
    - Layout.tsx contains `MIII-AIM` text in header section
    - Layout.tsx contains `#D9C27A` gold color for the `×` separator
    - Layout.tsx header includes both `⚡ Aetherium` and `MIII-AIM` text
    - Layout.tsx footer contains `⊹ Powered by MIII-AIM Engine`
    - Layout.tsx footer uses gold glyph (`#D9C27A`) before the power text
    - Layout.tsx footer styling includes muted text at `0.8em` with top border separator
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'MIII-AIM' web/components/Layout.tsx && grep -q 'D9C27A' web/components/Layout.tsx && grep -q 'Powered by MIII-AIM' web/components/Layout.tsx && grep -q 'Aetherium' web/components/Layout.tsx && echo "HEADER_FOOTER VERIFIED" || echo "HEADER_FOOTER NEEDS FIX"</automated>
  </verify>
  <done>
    Layout.tsx renders header with "⚡ Aetherium × MIII-AIM" (gold × separator, muted MIII-AIM text) and footer with "⊹ Powered by MIII-AIM Engine" (gold glyph, centered, bordered).
  </done>
</task>

<task type="auto">
  <name>task 2: Verify landing page architecture footnote</name>
  <files>web/pages/index.tsx</files>
  <read_first>web/pages/index.tsx</read_first>
  <action>
    Verify that `web/pages/index.tsx` contains the MIII-AIM architecture footnote (already implemented per discuss-phase). Check per UI-SPEC.md:

    1. **Footnote copy** — Below the main test section (typically before closing container), must render: `⊹ Architecture: MIII-AIM Sovereign Engine v1.0 · Identity-routed via Aetherium Crystal Core`.
    2. **Styling** — Gold `⊹` glyph (`#D9C27A`), gold `·` dot separator, muted text (`#9ca3af`, `0.8em`), separated by `1px solid #e5e7eb` top border with `40px` margin-top.
    3. **Position** — Below the identity test section, above the closing container tag.

    If any check fails, apply the fix using inline React.CSSProperties following existing page patterns.
  </action>
  <acceptance_criteria>
    - `web/pages/index.tsx` contains `Architecture: MIII-AIM Sovereign Engine v1.0`
    - `web/pages/index.tsx` contains `Identity-routed via Aetherium Crystal Core`
    - `web/pages/index.tsx` contains gold `#D9C27A` for decorative elements in footnote
    - Footnote is positioned after the test/submit section but before container close
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Architecture: MIII-AIM' web/pages/index.tsx && grep -q 'Identity-routed via Aetherium Crystal Core' web/pages/index.tsx && grep -q 'D9C27A' web/pages/index.tsx && echo "FOOTNOTE VERIFIED" || echo "FOOTNOTE NEEDS FIX"</automated>
  </verify>
  <done>
    Landing page displays architecture footnote with MIII-AIM Sovereign Engine v1.0 attribution and Aetherium Crystal Core routing note, with gold decorative elements.
  </done>
</task>

<task type="auto">
  <name>task 3: Verify button gold variant</name>
  <files>web/components/Button.tsx</files>
  <read_first>web/components/Button.tsx</read_first>
  <action>
    Verify that `web/components/Button.tsx` accepts and renders a `variant="gold"` prop (already implemented per discuss-phase). Check per UI-SPEC.md:

    1. **Variant type** — The `variant` prop must accept `'primary' | 'gold'` (string union type).
    2. **Gold styles** — `variant="gold"` must apply `backgroundColor: '#D9C27A'` (gold) and `color: '#0A0A0A'` (obsidian).
    3. **Primary default** — Default `variant="primary"` must render with indigo `#4F46E5` background (unchanged from original).
    4. **Behavior** — Gold variant must behave identically to primary variant (onClick, disabled, children all work the same).

    If any check fails, apply the fix by extending the variant styles map.
  </action>
  <acceptance_criteria>
    - `web/components/Button.tsx` has variant type accepting `'primary' | 'gold'`
    - `web/components/Button.tsx` has gold style block with `backgroundColor: '#D9C27A'` and `color: '#0A0A0A'`
    - Gold variant renders with same button structure as primary (same padding, border-radius, font-size)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "variant.*gold\|'gold'" web/components/Button.tsx && grep -q "'#D9C27A'\|#D9C27A" web/components/Button.tsx && grep -q "'#0A0A0A'\|#0A0A0A" web/components/Button.tsx && echo "BUTTON VERIFIED" || echo "BUTTON NEEDS FIX"</automated>
  </verify>
  <done>
    Button component accepts `variant="gold"` with gold (#D9C27A) background and obsidian (#0A0A0A) text, fully functional matching primary variant behavior.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — UI brand layer only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | T | web/components/* | accept | Visual brand elements — no runtime security impact |
</threat_model>

<verification>
- [x] Task 1: Layout header shows "⚡ Aetherium × MIII-AIM" with gold separator (grep passes)
- [x] Task 1: Layout footer shows "⊹ Powered by MIII-AIM Engine" with gold glyph (grep passes)
- [x] Task 2: Landing page footnote with MIII-AIM Sovereign Engine attribution (grep passes)
- [x] Task 3: Button component has variant="gold" with gold/obsidian colors (grep passes)
</verification>

<success_criteria>
- web/components/Layout.tsx has MIII-AIM header and footer brand elements
- web/pages/index.tsx has MIII-AIM architecture footnote
- web/components/Button.tsx supports variant="gold" prop
- All grep verification commands return positive confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/12-miii-aim-brand-melody/12-02-SUMMARY.md`
</output>
