---
phase: 12-miii-aim-brand-melody
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/docker-compose.yml
  - vercel.json
  - api/src/index.ts
  - api/src/services/orchestrator.ts
  - api/src/services/agent-builder.ts
  - api/src/routes/docs.ts
autonomous: true
requirements: [D-01, D-02]
user_setup: []

must_haves:
  truths:
    - "Docker Compose uses MongoDB 7 with healthcheck, Redis 7-alpine with healthcheck, correct env_file path"
    - "Vercel config exists at project root with rootDirectory: web and framework: nextjs"
    - "API serves GET / root route for Google Cloud Run"
    - "requireToolUse is conditional on tools.length > 0 in both orchestrator and agent-builder"
    - "Swagger UI setup handler stored as variable to avoid overload resolution error"
  artifacts:
    - path: "infra/docker-compose.yml"
      provides: "Fixed Docker Compose configuration"
      contains: "service_healthy"
    - path: "vercel.json"
      provides: "Vercel deployment configuration"
      contains: "rootDirectory"
    - path: "api/src/index.ts"
      provides: "Express app with root route"
      contains: 'app.get("/"'
    - path: "api/src/services/orchestrator.ts"
      provides: "requireToolUse conditional fix"
      contains: "tools.length > 0"
    - path: "api/src/services/agent-builder.ts"
      provides: "requireToolUse conditional fix"
      contains: "tools.length > 0"
    - path: "api/src/routes/docs.ts"
      provides: "Swagger type resolution fix"
      contains: "const setupHandler"
  key_links:
    - from: "infra/docker-compose.yml"
      to: ".env.local"
      via: "env_file directive pointing to project root env"
      pattern: "env_file.*\\.env\\.local"

---

<objective>
Verify all infrastructure and code-quality fixes applied during Phase 12 are correct and complete. This covers Docker Compose healthchecks, Vercel deployment config, Google Cloud root route, requireToolUse conditional fix, and Swagger type resolution.

Purpose: Ensure the development/deployment infrastructure is robust (healthchecked services), the API can deploy on Google Cloud Run (root route), and two code-quality issues are resolved (conditional requireToolUse, Swagger type fix).
Output: Verified infrastructure fixes, verified code-quality improvements.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-miii-aim-brand-melody/12-CONTEXT.md

<interfaces>
<!-- Key function signatures referenced in infrastructure files -->

From api/src/services/orchestrator.ts (pick() policy interface):
```typescript
export interface ProviderPolicy {
  requireToolUse?: boolean;
  requireEmbeddings?: boolean;
  // ... other fields
}
```

From api/src/services/agent-builder.ts:
```typescript
export class AgentBuilder {
  private tools: Tool[];
  // ...
  async executePlan(req: ExecutePlanRequest): Promise<ReasonResponse> {
    // policy: { requireToolUse: this.tools.length > 0 }
  }
}
```

From api/src/index.ts:
```typescript
// Root route serves landing page for Google Cloud Run
app.get("/", (req, res) => { ... });
```

From vercel.json:
```json
{
  "rootDirectory": "web",
  "framework": "nextjs"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Verify Docker Compose infrastructure fixes</name>
  <files>infra/docker-compose.yml</files>
  <read_first>infra/docker-compose.yml</read_first>
  <action>
    Verify that `infra/docker-compose.yml` has all infrastructure fixes applied (already implemented per discuss-phase). Check:

    1. **MongoDB image** — Must use `mongo:7` (not `mongo:6`).
    2. **MongoDB healthcheck** — Must include a healthcheck block testing `mongosh --eval 'db.runCommand("ping").ok'` with `interval`, `timeout`, `retries`, and `start_period`.
    3. **Redis image** — Must use `redis:7-alpine` (not bare `redis`).
    4. **Redis healthcheck** — Must include a healthcheck block testing `redis-cli ping` with `interval`, `timeout`, `retries`, and `start_period`.
    5. **env_file path** — Services that need environment must use `env_file: ../.env.local` (correct relative path from `infra/` directory).
    6. **depends_on** — Services depending on MongoDB/Redis must use `condition: service_healthy` (not just service name).

    If any check fails, apply the fix to the Docker Compose file.
  </action>
  <acceptance_criteria>
    - `docker-compose.yml` uses `mongo:7` image tag (not mongo:6)
    - `docker-compose.yml` has MongoDB healthcheck with `mongosh --eval` command
    - `docker-compose.yml` uses `redis:7-alpine` image tag
    - `docker-compose.yml` has Redis healthcheck with `redis-cli ping` command
    - `docker-compose.yml` uses `env_file: ../.env.local` for services needing environment
    - `docker-compose.yml` uses `condition: service_healthy` in depends_on blocks for mongo/redis
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'mongo:7' infra/docker-compose.yml && grep -q 'mongosh.*ping' infra/docker-compose.yml && grep -q 'redis:7-alpine' infra/docker-compose.yml && grep -q 'redis-cli ping' infra/docker-compose.yml && grep -q 'env_file: ../.env.local' infra/docker-compose.yml && grep -q 'condition: service_healthy' infra/docker-compose.yml && echo "DOCKER VERIFIED" || echo "DOCKER NEEDS FIX"</automated>
  </verify>
  <done>
    Docker Compose has MongoDB 7 with healthcheck, Redis 7-alpine with healthcheck, correct env_file path, and depends_on uses service_healthy conditions.
  </done>
</task>

<task type="auto">
  <name>task 2: Verify Vercel config and Google Cloud root route</name>
  <files>vercel.json, api/src/index.ts</files>
  <read_first>vercel.json, api/src/index.ts</read_first>
  <action>
    Verify two infrastructure fixes (already implemented per discuss-phase):

    1. **Vercel config** — `vercel.json` at project root must contain:
       - `"rootDirectory": "web"` — tells Vercel the Next.js app lives in `web/` directory
       - `"framework": "nextjs"` — explicitly sets framework to Next.js
       This file does NOT exist in `web/` — only at project root.

    2. **Google Cloud root route** — `api/src/index.ts` must contain a `GET /` route handler:
       - `app.get("/", (req, res) => { ... })` that serves a simple HTML landing page
       - Must include at minimum a title/heading identifying Aetherium API
       - Must return HTML content type (via `res.send()` with HTML string)
       - Placed before the 404 fallback but after other route registrations

    If either is missing, apply the fix.
  </action>
  <acceptance_criteria>
    - `vercel.json` at project root has `"rootDirectory": "web"`
    - `vercel.json` at project root has `"framework": "nextjs"`
    - `api/src/index.ts` has `app.get("/", ...)` root route
    - Root route handler sends HTML response (not JSON)
  </acceptance_criteria>
  <verify>
    <automated>test -f vercel.json && grep -q 'rootDirectory' vercel.json && grep -q 'framework' vercel.json && grep -q 'app.get.*"/"' api/src/index.ts && echo "VERCEL_GCP VERIFIED" || echo "VERCEL_GCP NEEDS FIX"</automated>
  </verify>
  <done>
    vercel.json exists at root with correct web directory and Next.js framework config; API serves GET / landing page for Google Cloud Run.
  </done>
</task>

<task type="auto">
  <name>task 3: Verify requireToolUse conditional and Swagger type fix</name>
  <files>api/src/services/orchestrator.ts, api/src/services/agent-builder.ts, api/src/routes/docs.ts</files>
  <read_first>api/src/services/orchestrator.ts, api/src/services/agent-builder.ts, api/src/routes/docs.ts</read_first>
  <action>
    Verify two code-quality fixes (already implemented per discuss-phase):

    1. **requireToolUse conditional** — In both `api/src/services/orchestrator.ts` and `api/src/services/agent-builder.ts`:
       - `requireToolUse` must be set to `tools.length > 0` (boolean expression, not always `true`)
       - In orchestrator: Check all calls to `providerRegistry.pick()` where `requireToolUse` is passed
       - In agent-builder: Check `executePlan()` and `streamPlan()` methods where policy is constructed
       - Also check: `if (tools.length > 0 && !provider.generateWithTools)` exists for null-safety

    2. **Swagger type fix** — In `api/src/routes/docs.ts`:
       - The return of `swaggerUi.setup(swaggerDocument)` must be stored in a variable: `const setupHandler = swaggerUi.setup(swaggerDocument);`
       - This resolves the overload resolution error that occurred when calling `swaggerUi.setup()` directly in the route handler

    If any check fails, apply the fix.
  </action>
  <acceptance_criteria>
    - `orchestrator.ts` passes `requireToolUse: tools.length > 0` (not bare `true`)
    - `orchestrator.ts` has `if (tools.length > 0 && !provider.generateWithTools)` guard
    - `agent-builder.ts` uses `this.tools.length > 0` for requireToolUse
    - `docs.ts` stores swaggerUi.setup result as `const setupHandler = swaggerUi.setup(...)`
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'tools.length > 0' api/src/services/orchestrator.ts | xargs test 2 -le && grep -q 'tools.length > 0' api/src/services/agent-builder.ts && grep -q 'setupHandler\s*=\s*swaggerUi\.setup' api/src/routes/docs.ts && echo "CODE_QUALITY VERIFIED" || echo "CODE_QUALITY NEEDS FIX"</automated>
  </verify>
  <done>
    requireToolUse is conditional on tools.length > 0 in both orchestrator.ts and agent-builder.ts; Swagger UI setupHandler is stored as a variable to resolve type overload error.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — infrastructure hardening only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | T | infra/docker-compose.yml | accept | Docker healthchecks improve reliability but not security boundaries |
</threat_model>

<verification>
- [x] Task 1: Docker Compose has MongoDB 7 + Redis 7-alpine + healthchecks + correct env_file (grep passes)
- [x] Task 2: Vercel config exists with rootDirectory + framework; Google Cloud root route serves HTML (grep passes)
- [x] Task 3: requireToolUse conditional (tools.length > 0) in orchestrator and agent-builder; Swagger type fix (grep passes)
</verification>

<success_criteria>
- infra/docker-compose.yml has healthchecked MongoDB 7 and Redis 7-alpine with correct env_file path
- vercel.json at project root has rootDirectory and framework config
- api/src/index.ts has GET / root route serving HTML landing page
- api/src/services/orchestrator.ts and agent-builder.ts use conditional requireToolUse
- api/src/routes/docs.ts stores swaggerUi.setup result as variable
- All grep verification commands return positive confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/12-miii-aim-brand-melody/12-03-SUMMARY.md`
</output>
