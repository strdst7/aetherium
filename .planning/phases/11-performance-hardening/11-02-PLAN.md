---
phase: 11-performance-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - api/src/tests/performance/latency.test.ts
  - api/jest.config.js
autonomous: true
requirements:
  - RE-06
must_haves:
  truths:
    - "p99 latency for identity-bound generation request is under 5 seconds total"
    - "Identity lookup and rule application latency remains under 200ms"
  artifacts:
    - path: "api/src/tests/performance/latency.test.ts"
      provides: "Latency validation tests for full pipeline and identity overhead"
      exports: []
      contains: "describe('Latency Validation'"
  key_links:
    - from: "api/src/tests/performance/latency.test.ts"
      to: "api/src/tests/helpers/benchmark.ts"
      via: "import { BenchmarkRunner }"
      pattern: "BenchmarkRunner"
    - from: "api/src/tests/performance/latency.test.ts"
      to: "api/src/tests/fixtures/performance-fixtures.ts"
      via: "import { DEFAULT_LATENCY_SCENARIO }"
      pattern: "DEFAULT_LATENCY_SCENARIO"
---

<objective>
Validate that the identity-bound generation pipeline meets the p99 latency target (<5s total) and that identity overhead (lookup + rule application) remains under the 200ms constraint.

Purpose: Phase 10 measured identity binding at ≤108ms, but no systematic p99 measurement exists for the full generation pipeline. This plan closes the performance verification gap for RE-06.
Output: Latency test file with p99 assertions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key prior art:
- `api/src/tests/pipeline.integration.test.ts` has a latency test for identity binding (≤200ms) using Date.now() measurement
- `api/src/tests/helpers/benchmark.ts` (from 11-01) provides BenchmarkRunner for systematic measurement
- `api/src/tests/fixtures/performance-fixtures.ts` (from 11-01) provides DEFAULT_LATENCY_SCENARIO
- Mock provider is deterministic and fast (no network latency), so measured latency reflects system overhead
</context>

<interfaces>
From api/src/tests/helpers/benchmark.ts:
```typescript
export class BenchmarkRunner {
  constructor(label: string);
  async run<T>(fn: () => Promise<T>, label?: string): Promise<T>;
  getP99(): number;
  getP95(): number;
  getMean(): number;
  summary(): object;
}
export async function measureLatency<T>(fn: () => Promise<T>): Promise<{ result: T; durationMs: number }>;
```

From api/src/tests/fixtures/performance-fixtures.ts:
```typescript
export interface PerformanceScenario { identities: SigilIdentity[]; requests: OrchestratorRequest[]; concurrentCount: number; }
export const DEFAULT_LATENCY_SCENARIO: PerformanceScenario;
```

From api/src/tests/setup.ts:
```typescript
// global.testDb is available in beforeAll/beforeEach
```

From api/src/tests/pipeline.integration.test.ts:
```typescript
// createOrchestrator() helper pattern with mock provider
// identityService.createIdentity() pattern
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: create latency validation tests</name>
  <files>api/src/tests/performance/latency.test.ts</files>
  <read_first>
    - api/src/tests/helpers/benchmark.ts (BenchmarkRunner API)
    - api/src/tests/fixtures/performance-fixtures.ts (scenario types)
    - api/src/tests/pipeline.integration.test.ts (orchestrator setup pattern)
    - api/src/tests/setup.ts (Jest global lifecycle)
    - api/jest.config.js (current test configuration)
  </read_first>
  <action>
    Create `api/src/tests/performance/latency.test.ts` with the following tests:

    1. **Test setup** (beforeAll):
       - Connect IdentityService to global.testDb.uri
       - Connect MemoryService to global.testDb.uri
       - Initialize AuditService
       - Initialize ProviderRegistry, IdentityBindingService, SymbolicAnchorLoader, MythicModule, IdentityConstraintEngine, SovereignHaloService
       - Register MockProviderFactory with deterministic=true
       - Create Orchestrator with all services
       - Store references in test-local variables (not global)

    2. **Test teardown** (afterAll):
       - Disconnect all services
       - Clear ProviderRegistry

    3. **"should measure p99 full pipeline latency under 5000ms"**:
       - Create one identity via identityService.createIdentity
       - Use BenchmarkRunner to run the full Orchestrator.process() 20 times
       - Each run: request with identity_anchor and a simple prompt
       - Assert getP99() < 5000
       - Assert getMean() < 3000 (mean should be comfortably under target)
       - Log benchmark.summary() for diagnostic output

    4. **"should measure identity binding latency under 200ms"**:
       - Create one identity
       - Use BenchmarkRunner to call identityBinding.resolve(identity.id) 20 times
       - Assert getP99() <= 200
       - Assert getMax() <= 200 (worst case must also meet constraint)

    5. **"should measure Sovereign Halo validation latency under 1000ms"**:
       - Create one identity with simple constraints
       - Generate mock output text
       - Use BenchmarkRunner to call sovereignHalo.validate() 20 times
       - Assert getP99() < 1000 (Halo should not add >1s to pipeline)

    6. Update `api/jest.config.js`:
       - Add `'!**/tests/performance/**/*.test.ts'` to testPathIgnorePatterns if not already covered by a broader pattern, OR ensure performance tests run with default `npm test` (they should run by default since they use the same mock infrastructure)
       - No exclusion needed if performance tests are fast with mock provider
  </action>
  <verify>
    <automated>cd api && npx tsc --noEmit src/tests/performance/latency.test.ts</automated>
  </verify>
  <done>
    - `latency.test.ts` compiles with zero TypeScript errors
    - Contains 3 describe blocks or it blocks covering: full pipeline p99, identity binding p99, halo validation p99
    - All assertions use BenchmarkRunner.getP99() or getMax()
    - Thresholds: pipeline <5000ms, binding <=200ms, halo <1000ms
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production | Tests must use test database and mock providers only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure | latency.test.ts | accept | Uses test fixtures with fake data |
| T-11-04 | Denial of Service | latency.test.ts | accept | Mock provider; no external calls |
</threat_model>

<verification>
- `cd api && npm test -- src/tests/performance/latency.test.ts` passes (with mock provider)
- `tsc --noEmit` on latency.test.ts returns 0
- Benchmark summary is logged to console for diagnostic review
</verification>

<success_criteria>
- p99 full pipeline latency assertion passes (<5000ms)
- p99 identity binding latency assertion passes (<=200ms)
- p99 Sovereign Halo validation assertion passes (<1000ms)
- Tests run in under 60 seconds total (mock provider is fast)
</success_criteria>

<output>
After completion, create `.planning/phases/11-performance-hardening/11-02-SUMMARY.md`
</output>
