---
phase: 11-performance-hardening
plan: 05
type: execute
wave: 3
depends_on:
  - 11-02
  - 11-03
  - 11-04
files_modified:
  - docs/DEPLOYMENT.md
  - README.md
  - api/package.json
  - web/package.json
autonomous: true
requirements:
  - RE-06
must_haves:
  truths:
    - "Deployment guide covers Docker Compose local setup and cloud deployment"
    - "README.md provides project overview, quickstart, and architecture summary"
    - "All environment variables are documented with descriptions and example values"
  artifacts:
    - path: "docs/DEPLOYMENT.md"
      provides: "Complete deployment guide"
      contains: "## Docker Compose"
    - path: "README.md"
      provides: "Project overview and quickstart"
      contains: "## Quickstart"
    - path: "api/package.json"
      provides: "API dependencies documented"
      min_changes: 0
    - path: "web/package.json"
      provides: "Web dependencies documented"
      min_changes: 0
  key_links:
    - from: "docs/DEPLOYMENT.md"
      to: "infra/docker-compose.yml"
      via: "references docker-compose"
      pattern: "docker-compose"
    - from: "README.md"
      to: "docs/API_INTEGRATION.md"
      via: "links to API guide"
      pattern: "API_INTEGRATION"
---

<objective>
Create a complete deployment guide covering local Docker Compose setup and cloud deployment options, update the README with project overview and quickstart, and run final verification to ensure all Phase 11 deliverables are complete.

Purpose: Phase 11 is the final hardening phase. Without deployment documentation, the system cannot be operated by others. The README is currently a placeholder (`# aetherium`) and must be replaced with a proper project overview.
Output: DEPLOYMENT.md, updated README.md, verified performance tests and documentation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key prior art:
- `infra/docker-compose.yml` has MongoDB, Redis, MinIO, API, and Web services
- `README.md` is currently a 1-line placeholder
- `docs/runbook.md` exists with demo/runbook content but no deployment instructions
- `docs/API_INTEGRATION.md` (from 11-04) provides API usage documentation
- Environment variables from AGENTS.md: MONGODB_URI, S3_*, REDIS_URL, API_PORT, WEB_PORT, NEXT_PUBLIC_API_URL, GEMINI_API_KEY (or Google Cloud credentials)
- CI exists at `.github/workflows/ci.yml` with api-tests and web-tests jobs
</context>

<interfaces>
From infra/docker-compose.yml:
```yaml
services:
  minio: image: minio/minio:latest, ports: 9000, 9001
  mongo: image: mongo:6.0, ports: 27017
  redis: image: redis:7, ports: 6379
  api: build: context: .., dockerfile: api/Dockerfile, ports: 8080
  web: build: context: .., dockerfile: web/Dockerfile, ports: 3000
```

From AGENTS.md:
```
Environment variables in .env.local:
- MONGODB_URI, S3_*, REDIS_URL, API_PORT, WEB_PORT, NEXT_PUBLIC_API_URL
- Gemini / Google Cloud credentials (to be added for v1)
```

From package.json (root):
```json
{ "scripts": { "test:api": "cd api && npm test", "test:web": "cd web && npm test", "test:all": "...", "test:e2e": "..." } }
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: create deployment guide and update README</name>
  <files>docs/DEPLOYMENT.md, README.md</files>
  <read_first>
    - infra/docker-compose.yml (current services and configuration)
    - AGENTS.md (environment variables and tech stack)
    - .github/workflows/ci.yml (CI pipeline for reference)
    - docs/API_INTEGRATION.md (from 11-04 — link to it)
  </read_first>
  <action>
    1. Create `docs/DEPLOYMENT.md` with:

       **Prerequisites**:
       - Node.js 20+, Docker & Docker Compose, MongoDB 6+ (or use Docker), Redis 7+ (or use Docker)

       **Local Development Setup**:
       - Step 1: Clone repository
       - Step 2: Create `.env.local` with all required environment variables (list each with description and example):
         ```
         MONGODB_URI=mongodb://localhost:27017/aetherium
         REDIS_URL=redis://localhost:6379
         API_PORT=8080
         WEB_PORT=3000
         NEXT_PUBLIC_API_URL=http://localhost:8080
         GEMINI_API_KEY=your_gemini_api_key_here
         MCP_SERVER_PATH=/path/to/mcp-server
         ```
       - Step 3: Install dependencies: `npm install` (root workspaces)
       - Step 4: Start infrastructure: `docker-compose -f infra/docker-compose.yml up mongo redis minio`
       - Step 5: Seed data (if needed): reference any seed scripts
       - Step 6: Start API: `cd api && npm run dev`
       - Step 7: Start Web: `cd web && npm run dev`
       - Step 8: Verify: `curl http://localhost:8080/health`

       **Docker Compose Full Stack**:
       - `docker-compose -f infra/docker-compose.yml up --build`
       - Services table: name, image, port, purpose
       - Note: API and web services build from local Dockerfiles; ensure Dockerfiles exist

       **Cloud Deployment Options**:
       - **Option A: Google Cloud Run** (containerized API + web, Cloud MongoDB Atlas, Memorystore Redis)
       - **Option B: AWS** (ECS/Fargate for API + web, DocumentDB or MongoDB Atlas, ElastiCache Redis)
       - **Option C: Self-managed VPS** (Docker Compose on single VM)
       - For each: brief architecture diagram description, env var differences, scaling notes

       **Environment Configuration Reference**:
       - Complete table of all env vars: name, required/optional, description, example

       **Health Checks & Monitoring**:
       - GET /health endpoint
       - GET /v1/info endpoint
       - Log inspection locations

       **Troubleshooting**:
       - MongoDB connection failures
       - Redis connection failures
       - Provider health check failures (Gemini API key missing)
       - Port conflicts

    2. Update `README.md` with:
       - Project title: "Aetherium — Sovereign, Identity-First AI Intelligence Platform"
       - One-paragraph description of core value (identity-consistent LLM outputs)
       - Architecture bullet list (Gemini + Agent Builder, MongoDB, Redis, Next.js web shell)
       - Quickstart section with 3-4 commands to get running
       - Links to docs: API_INTEGRATION.md, DEPLOYMENT.md, runbook.md
       - Tech stack table matching AGENTS.md
       - Badges: CI status placeholder, license ISC
       - Contributing notes (tests, conventions)
  </action>
  <verify>
    <automated>grep -c "Docker Compose" docs/DEPLOYMENT.md && grep -c "Quickstart" README.md && grep -c "Aetherium" README.md</automated>
  </verify>
  <done>
    - `docs/DEPLOYMENT.md` exists and is >150 lines
    - Contains sections: Local Development, Docker Compose, Cloud Options, Env Reference, Health Checks, Troubleshooting
    - `README.md` is >50 lines with Quickstart, Architecture, and Links sections
    - README contains link to docs/API_INTEGRATION.md and docs/DEPLOYMENT.md
  </done>
</task>

<task type="auto">
  <name>task 2: final verification and documentation completeness check</name>
  <files>api/package.json, web/package.json</files>
  <read_first>
    - api/openapi.yml (verify examples are present)
    - docs/API_INTEGRATION.md (verify completeness)
    - docs/DEPLOYMENT.md (verify completeness)
    - README.md (verify completeness)
    - api/src/tests/performance/latency.test.ts (from 11-02)
    - api/src/tests/performance/concurrency.test.ts (from 11-03)
  </read_first>
  <action>
    1. **Run performance tests**:
       - `cd api && npm test -- src/tests/performance/latency.test.ts --detectOpenHandles --forceExit`
       - `cd api && npm test -- src/tests/performance/concurrency.test.ts --detectOpenHandles --forceExit`
       - Record results in a verification log

    2. **Documentation completeness check** (automated grep gates):
       - Verify `api/openapi.yml` has `examples:` blocks for: /v1/reason, /identity/register, /identity/{id}, /audit, /v1/memory/search
       - Verify `docs/API_INTEGRATION.md` contains: "curl", "fetch", "Accept-Version", "Problem Details", "identity_anchor"
       - Verify `docs/DEPLOYMENT.md` contains: "MONGODB_URI", "REDIS_URL", "docker-compose", "health"
       - Verify `README.md` contains: "Quickstart", "Architecture", "API_INTEGRATION", "DEPLOYMENT"

    3. **Update package.json metadata** (if needed):
       - `api/package.json`: add `repository` and `bugs` fields pointing to project repo (if known, otherwise skip)
       - `web/package.json`: same
       - No functional changes to dependencies or scripts

    4. **Create verification report**:
       - Write a brief markdown summary to `.planning/phases/11-performance-hardening/11-VERIFICATION.md` with:
         - Performance test results (pass/fail, p99 values observed)
         - Documentation checklist (all items verified with ✓)
         - Any warnings or notes
  </action>
  <verify>
    <automated>cd api && npx jest --testPathPattern='performance/' --detectOpenHandles --forceExit --passWithNoTests</automated>
  </verify>
  <done>
    - Performance tests pass (latency and concurrency)
    - OpenAPI examples verified present for all major endpoints
    - API_INTEGRATION.md contains all required keywords
    - DEPLOYMENT.md contains all required keywords
    - README.md contains all required keywords
    - 11-VERIFICATION.md exists with pass/fail results
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → public | Deployment docs are public-facing; must not contain secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Information Disclosure | DEPLOYMENT.md | mitigate | Use placeholder values for secrets (e.g., `your_gemini_api_key_here`); never commit real credentials |
| T-11-10 | Information Disclosure | README.md | accept | Only public project info; no internal architecture secrets |
</threat_model>

<verification>
- DEPLOYMENT.md does not contain any real API keys or passwords (grep for patterns like `key=sk-`, `password=real`)
- README.md links correctly to docs/ files
- Performance tests pass in CI-compatible mode
- All package.json files remain valid JSON
</verification>

<success_criteria>
- Deployment guide enables a new developer to set up the full stack locally within 15 minutes
- README.md provides clear project identity and quickstart path
- All public API endpoints have documented examples
- Performance tests confirm p99 <5s and identity overhead ≤200ms
- No documentation gaps remain for v1 API integrators
</success_criteria>

<output>
After completion, create `.planning/phases/11-performance-hardening/11-05-SUMMARY.md`
</output>
