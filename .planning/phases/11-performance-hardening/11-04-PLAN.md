---
phase: 11-performance-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - api/openapi.yml
  - docs/API_INTEGRATION.md
autonomous: true
requirements:
  - AG-07
  - AG-08
  - ID-01
  - ID-02
  - ID-03
  - ID-04
  - ID-05
  - ID-06
  - AUD-03
must_haves:
  truths:
    - "OpenAPI specification includes complete request and response examples for every public endpoint"
    - "API integration guide exists with curl and JavaScript examples for all major flows"
  artifacts:
    - path: "api/openapi.yml"
      provides: "Enriched OpenAPI 3.0 spec with examples"
      contains: "examples:"
    - path: "docs/API_INTEGRATION.md"
      provides: "Developer integration guide with examples"
      contains: "## Endpoints"
  key_links:
    - from: "docs/API_INTEGRATION.md"
      to: "api/openapi.yml"
      via: "references OpenAPI spec"
      pattern: "openapi.yml"
---

<objective>
Ensure the API is fully documented with request/response examples for all public endpoints, and create a developer integration guide with practical curl and JavaScript examples.

Purpose: The OpenAPI spec exists but lacks complete examples. An integration guide is missing entirely. Phase 11 requires documentation so API integrators can adopt Aetherium without reading source code.
Output: Enriched OpenAPI spec, API integration guide markdown.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key prior art:
- `api/openapi.yml` exists with schemas but minimal examples (only basic HealthResponse)
- `api/src/types/api-contracts.ts` has TypeScript types matching OpenAPI schemas
- Public endpoints from openapi.yml: /health, /v1/info, /v1/reason, /v1/memory/search, /v1/memory/upsert, /identity, /identity/{id}, /identity/{id}/versions, /audit, /v1/multi-agent/reason
- `web/src/lib/api-client.ts` has JavaScript fetch patterns for the web client
</context>

<interfaces>
From api/openapi.yml (existing structure):
```yaml
paths:
  /health: ...
  /v1/info: ...
  /v1/reason: ...
  /v1/memory/search: ...
  /v1/memory/upsert: ...
  /identity: ...
  /identity/{id}: ...
  /identity/{id}/versions: ...
  /audit: ...
```

From api/src/types/api-contracts.ts:
```typescript
export interface ReasonRequest {
  identity_anchor?: string;
  messages: Array<{ role: string; content: string }>;
  mode?: "tool" | "task";
}

export interface ReasonResponse {
  text: string;
  reasoning?: ReasoningPayload;
  validationReport?: ValidationReport;
  identity?: SigilIdentity;
}
```

From api/src/controllers/identity.ts:
```typescript
// POST /identity/register -> { id, name, developerId, sigilHash, version }
// GET /identity/{id} -> SigilIdentity
// PUT /identity/{id} -> SigilIdentity (updated)
// GET /identity -> SigilIdentity[]
// GET /identity/{id}/versions -> IdentityVersion[]
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: enrich OpenAPI spec with complete examples</name>
  <files>api/openapi.yml</files>
  <read_first>
    - api/openapi.yml (current spec to identify which endpoints lack examples)
    - api/src/types/api-contracts.ts (TypeScript types for accuracy)
    - api/src/controllers/identity.ts (request/response shapes)
    - api/src/controllers/audit.ts (audit query params)
  </read_first>
  <action>
    Update `api/openapi.yml` to add complete `examples:` blocks under every public endpoint's requestBody and responses:

    1. **POST /v1/reason**:
       - Request example: `{ "identity_anchor": "id_abc123", "messages": [{"role":"user","content":"Explain quantum computing"}], "mode": "tool" }`
       - Response 200 example: `{ "text": "Quantum computing harnesses...", "reasoning": { "steps": [...], "toolsUsed": [...] }, "validationReport": { "status": "passed", "checks": [...], "confidence": 0.95 }, "identity": { "id": "id_abc123", "name": "Research Assistant" } }`
       - Response 400 example: `{ "type": "https://aetherium.io/errors/invalid-request", "title": "Invalid Request", "status": 400, "detail": "messages array is required" }`

    2. **POST /v1/memory/search**:
       - Request example: `{ "query": "quantum computing papers", "topK": 5, "identity_anchor": "id_abc123" }`
       - Response example: `{ "results": [{ "content": "...", "score": 0.92, "identityId": "id_abc123" }] }`

    3. **POST /v1/memory/upsert**:
       - Request example: `{ "content": "New research finding...", "embedding": [0.1, 0.2, ...], "identity_anchor": "id_abc123" }`

    4. **POST /identity/register**:
       - Request example: `{ "name": "Research Assistant", "developerId": "dev@example.com", "config": { "preferredProvider": "gemini", "customRules": ["Use academic tone"] } }`
       - Response 201 example: `{ "id": "id_xyz789", "name": "Research Assistant", "developerId": "dev@example.com", "sigilHash": "a1b2c3...", "version": 1, "createdAt": "2026-01-01T00:00:00Z" }`

    5. **GET /identity/{id}**:
       - Response example: full SigilIdentity object with versions array

    6. **PUT /identity/{id}**:
       - Request example: `{ "name": "Updated Name", "config": { "preferredProvider": "gemini" } }`
       - Response example: updated SigilIdentity with incremented version

    7. **GET /identity/{id}/versions**:
       - Response example: `{ "versions": [{ "version": 1, "snapshot": { ... }, "createdAt": "..." }] }`

    8. **GET /audit**:
       - Add query params with examples: `?identity_id=id_abc123&from=2026-01-01&to=2026-12-31&page=1&limit=20`
       - Response example: `{ "records": [{ "recordId": "...", "identityId": "...", "prompt": "...", "output": "...", "timestamp": "..." }], "total": 42 }`

    9. **POST /v1/multi-agent/reason**:
       - Request example: `{ "identity_anchor": "id_abc123", "messages": [{"role":"user","content":"Analyze this"}], "agents": ["archivist", "narrator"] }`
       - Response example: `{ "councilOutput": { "consensus": "...", "agentOutputs": [...] }, "validationReport": { ... } }`

    Do NOT modify existing schema definitions — only add `examples:` entries under existing content blocks.
    Validate that the YAML is well-formed after editing.
  </action>
  <verify>
    <automated>cd api && python3 -c "import yaml; yaml.safe_load(open('openapi.yml'))" 2>/dev/null || node -e "const yaml=require('js-yaml'); yaml.load(require('fs').readFileSync('openapi.yml','utf8'))" 2>/dev/null || echo "YAML validation skipped (no parser available)"</automated>
  </verify>
  <done>
    - Every public endpoint in openapi.yml has at least one request example (for POST/PUT) and one response example (for 200/201)
    - Examples include realistic field values matching the schema types
    - No schema definitions were removed or altered (only examples added)
    - YAML remains parseable
  </done>
</task>

<task type="auto">
  <name>task 2: create API integration guide</name>
  <files>docs/API_INTEGRATION.md</files>
  <read_first>
    - api/openapi.yml (endpoint listing and schemas)
    - web/src/lib/api-client.ts (JS fetch patterns)
    - api/src/middleware/version-negotiation.ts (version header requirements)
  </read_first>
  <action>
    Create `docs/API_INTEGRATION.md` with the following sections:

    1. **Getting Started**:
       - Base URL: `http://localhost:8080` (local) or production URL
       - Required headers: `Content-Type: application/json`, `Accept-Version: 1.0.0` (or `X-API-Version: 1.0.0`)
       - Authentication: None for v1 (API key auth is v2 per REQUIREMENTS.md)

    2. **Identity Registration**:
       - curl example for POST /identity/register
       - JavaScript fetch example
       - Expected response fields explained

    3. **Submitting a Reasoning Request**:
       - curl example for POST /v1/reason with identity_anchor
       - JavaScript fetch example
       - How to interpret ReasonResponse (text, reasoning, validationReport)
       - How to handle validation failures

    4. **Memory Management**:
       - curl example for POST /v1/memory/upsert
       - curl example for POST /v1/memory/search with identity scoping

    5. **Audit Trail**:
       - curl example for GET /audit with query params
       - How to verify immutability (no PUT/DELETE endpoints)

    6. **Error Handling**:
       - Problem Details format (RFC 7807)
       - Common error codes and how to resolve them
       - Version negotiation failure (404 with ProblemDetails)

    7. **Versioning**:
       - How version negotiation works (Accept-Version, X-API-Version, ?apiVersion)
       - Current version: 1.0.0
       - Backward compatibility guarantee (additive changes only)

    8. **Rate Limits & Performance**:
       - Expected latency: identity binding ≤200ms, full pipeline p99 <5s
       - Concurrent request guidance

    Use copy-paste ready curl and JavaScript examples. Do NOT use placeholder text like "your-api-key" unless explicitly relevant.
  </action>
  <verify>
    <automated>grep -c "curl" docs/API_INTEGRATION.md && grep -c "fetch" docs/API_INTEGRATION.md</automated>
  </verify>
  <done>
    - `docs/API_INTEGRATION.md` exists and is >100 lines
    - Contains sections for all 8 topics listed above
    - Includes at least one curl example per endpoint group
    - Includes at least one JavaScript fetch example per endpoint group
    - Mentions version negotiation headers (Accept-Version or X-API-Version)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → public | Documentation is public-facing; must not expose internal architecture secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Information Disclosure | API_INTEGRATION.md | accept | Only documents public endpoints; no internal secrets |
| T-11-08 | Repudiation | openapi.yml examples | accept | Examples use fake data; no production credentials |
</threat_model>

<verification>
- `docs/API_INTEGRATION.md` is readable markdown with working headers
- Every public endpoint in openapi.yml has an example block
- curl examples are syntactically valid (quotes balanced, method + URL + headers correct)
</verification>

<success_criteria>
- OpenAPI spec contains complete request/response examples for all 9+ public endpoints
- API integration guide covers identity registration, reasoning, memory, audit, errors, and versioning
- All examples use realistic values matching the schema definitions
- Documentation is suitable for a new API integrator to make their first successful request
</success_criteria>

<output>
After completion, create `.planning/phases/11-performance-hardening/11-04-SUMMARY.md`
</output>
