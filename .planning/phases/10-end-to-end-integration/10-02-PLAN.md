---
phase: 10-end-to-end-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/tests/setup.ts
  - api/src/tests/pipeline.integration.test.ts
  - api/src/tests/services/audit-integration.test.ts
autonomous: true
requirements:
  - AG-01
  - AG-02
  - AG-03
  - AG-04
  - AG-05
  - AG-06
  - AG-07
  - AG-08
  - ID-01
  - ID-02
  - ID-03
  - ID-04
  - ID-05
  - ID-06
  - RE-01
  - RE-02
  - RE-03
  - RE-04
  - RE-05
  - RE-06
  - MY-01
  - MY-02
  - MY-03
  - MY-04
  - MY-05
  - SH-01
  - SH-02
  - SH-03
  - SH-04
  - SH-05
  - AUD-01
  - AUD-02
  - AUD-03
  - AUD-04
must_haves:
  truths:
    - "Full pipeline test passes: register identity → submit prompt → receive identity-bound output → verify audit record exists"
    - "Audit record contains all required fields: identity_id, prompt, output, reasoning_trace, validation_report, timestamp, version"
    - "Identity binding occurs before reasoning (latency measured ≤200ms)"
    - "Memory retrieval is scoped to the submitted identity_anchor"
  artifacts:
    - path: "api/src/tests/setup.ts"
      provides: "Jest global setup and teardown for integration tests"
      exports: []
    - path: "api/src/tests/pipeline.integration.test.ts"
      provides: "Full end-to-end pipeline integration test"
      exports: []
    - path: "api/src/tests/services/audit-integration.test.ts"
      provides: "Audit service integration with pipeline"
      exports: []
  key_links:
    - from: "api/src/tests/pipeline.integration.test.ts"
      to: "api/src/tests/helpers/test-database.ts"
      via: "import { createTestDatabase }"
      pattern: "createTestDatabase"
    - from: "api/src/tests/pipeline.integration.test.ts"
      to: "api/src/tests/fixtures/identity-factory.ts"
      via: "import { createIdentity }"
      pattern: "createIdentity"
    - from: "api/src/tests/pipeline.integration.test.ts"
      to: "api/src/tests/helpers/mock-provider-factory.ts"
      via: "import { MockProviderFactory }"
      pattern: "MockProviderFactory"
---

<objective>
Implement the primary full-pipeline integration test: register a SigilIdentity, submit a reasoning request through the Orchestrator, verify the output is identity-bound, and confirm an immutable AuditRecord was created.

Purpose: This is the central proof of Phase 10 — it validates that ALL prior phases (1-9) integrate correctly end-to-end. A single green test here proves identity registration, binding, mythic shaping, sovereign validation, and audit immutability work together.
Output: Jest setup file, full pipeline integration test, audit integration test.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key prior art from STATE.md:
- Orchestrator.process() loads identity, generates with mythic context, validates with Sovereign Halo, and saves audit records
- IdentityBindingService resolves identity_anchor to SigilIdentity with ≤200ms target
- AuditService.save() is best-effort (doesn't block on failure)
- All services are wired together in api/src/bootstrap/providers.ts
</context>

<interfaces>
From api/src/services/orchestrator.ts:
```typescript
export class Orchestrator {
  constructor(
    memoryService: MemoryService,
    providerRegistry: ProviderRegistry,
    identityBinding?: IdentityBindingService,
    mythicModule?: MythicModule,
    sovereignHalo?: SovereignHaloService,
    auditService?: AuditService
  );
  async process(req: OrchestratorRequest): Promise<OrchestratorResponse>;
}

export interface OrchestratorRequest {
  identity_anchor: string;
  messages: Array<{ role: string; content: string }>;
  maxTokens?: number;
  temperature?: number;
  policy?: { requireEmbeddings?: boolean; preferLocal?: boolean; requireToolUse?: boolean };
  memoryAlpha?: number;
  memoryK?: number;
}

export interface OrchestratorResponse {
  id: string;
  text: string;
  context: OrchestratorContext;
  reflectiveResult?: any;
  refinedCandidate?: any;
  toolCalls?: ToolCall[];
  validationReport?: ValidationReport;
}
```

From api/src/services/identity-service.ts:
```typescript
export class IdentityService {
  async create(data: Partial<SigilIdentity>): Promise<SigilIdentity>;
  async findById(id: string): Promise<SigilIdentity | null>;
}
```

From api/src/services/audit-service.ts:
```typescript
export class AuditService {
  async save(record: AuditRecordCreate): Promise<AuditRecord | null>;
  async query(query: AuditQuery): Promise<AuditListResponse>;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: create jest global setup for integration tests</name>
  <files>api/src/tests/setup.ts</files>
  <read_first>
    - api/jest.config.js (current setupFilesAfterEnv config)
    - api/src/tests/helpers/test-database.ts (TestDatabase interface from 10-01)
    - api/src/bootstrap/providers.ts (to understand service wiring)
  </read_first>
  <action>
    Create `api/src/tests/setup.ts`:
    - Import { createTestDatabase } from './helpers/test-database'
    - Import { MongoClient } from 'mongodb'
    - Declare global testDb: TestDatabase | undefined
    - beforeAll(async () => { ... }):
      - Create TestDatabase instance
      - Call setup() to connect and prepare collections
      - Store in global.testDb
      - Log "[Test Setup] Connected to test database"
    - afterAll(async () => { ... }):
      - If global.testDb exists, call teardown()
      - Log "[Test Setup] Disconnected from test database"
    - beforeEach(async () => { ... }):
      - If global.testDb exists, call reset() to clear collections between tests
      - Log "[Test Setup] Reset test database"
    - Export nothing (jest setup file runs for side effects)
    - Add `global.d.ts` in api/src/tests/ if needed:
      ```typescript
      import { TestDatabase } from './helpers/test-database';
      declare global {
        var testDb: TestDatabase | undefined;
      }
      export {};
      ```
  </action>
  <verify>
    <automated>cd api && npx tsc --noEmit src/tests/setup.ts</automated>
  </verify>
  <done>
    - setup.ts compiles without errors
    - setup.ts exports global.d.ts for TypeScript awareness of global.testDb
    - jest.config.js references setup.ts in setupFilesAfterEnv
  </done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement full pipeline integration test</name>
  <files>api/src/tests/pipeline.integration.test.ts</files>
  <read_first>
    - api/src/services/orchestrator.ts (Orchestrator constructor and process method)
    - api/src/services/identity-service.ts (IdentityService.create signature)
    - api/src/services/audit-service.ts (AuditService.save and query signatures)
    - api/src/services/provider-registry.ts (ProviderRegistry.register and pick)
    - api/src/tests/fixtures/identity-factory.ts (createIdentity from 10-01)
    - api/src/tests/helpers/mock-provider-factory.ts (MockProviderFactory from 10-01)
    - api/src/tests/helpers/test-database.ts (TestDatabase from 10-01)
    - api/src/bootstrap/providers.ts (to understand how services are wired)
    - api/src/types/api-contracts.ts (ReasonRequest shape)
    - api/src/types/audit.ts (AuditRecord, AuditQuery shapes)
  </read_first>
  <behavior>
    - Test 1: "should pass full pipeline: register identity → generate → audit record exists"
      - Create identity via IdentityService using test database
      - Register mock provider with ProviderRegistry
      - Call orchestrator.process() with identity_anchor
      - Assert response.text is non-empty
      - Assert response.validationReport exists
      - Query audit records by identity_id
      - Assert exactly 1 audit record exists
      - Assert audit record contains: identityId, prompt, output, reasoningTrace, validationReport, timestamp, hash
    - Test 2: "should apply identity constraints during reasoning"
      - Create identity with forbiddenBehaviors: ["no technical jargon"]
      - Register mock provider that returns text containing "technical jargon"
      - Call orchestrator.process()
      - Assert Sovereign Halo validationReport.status is "refine" or "reject"
      - Assert audit record exists with validationReport attached
    - Test 3: "should scope memory retrieval to identity"
      - Create two identities: identityA and identityB
      - Upsert memory for identityA via MemoryService
      - Call orchestrator.process() for identityB
      - Assert context.relevantMemories is empty (no cross-identity leakage)
    - Test 4: "should measure identity binding latency ≤ 200ms"
      - Create identity
      - Measure time for identityBinding.resolve(identity_anchor)
      - Assert elapsed ≤ 200ms
  </behavior>
  <action>
    Create `api/src/tests/pipeline.integration.test.ts`:
    - Import required services, fixtures, helpers
    - Describe block: "Full Pipeline Integration"
    - For each test:
      1. Use global.testDb to get database connection
      2. Create fresh service instances wired to test database
      3. Use createIdentity() for test data
      4. Use MockProviderFactory for deterministic LLM responses
      5. Register mock provider with ProviderRegistry
      6. Execute orchestrator.process()
      7. Make assertions on response and audit records
    - Each test must be independent (use beforeEach to reset database)
    - All async operations must use await
    - Use expect().toBe(), expect().toBeDefined(), expect().toBeLessThanOrEqual() as appropriate
  </action>
  <verify>
    <automated>cd api && npx jest src/tests/pipeline.integration.test.ts --passWithNoTests --detectOpenHandles --forceExit 2>&1 | head -50</automated>
  </verify>
  <done>
    - pipeline.integration.test.ts compiles and runs (may fail if services aren't fully wired yet — that's expected)
    - 4 test cases are defined covering: full pipeline, identity constraints, memory scoping, latency
    - Tests use test database helper and fixtures exclusively
  </done>
</task>

<task type="auto" tdd="true">
  <name>task 3: implement audit service integration test</name>
  <files>api/src/tests/services/audit-integration.test.ts</files>
  <read_first>
    - api/src/services/audit-service.ts (AuditService implementation)
    - api/src/types/audit.ts (AuditRecordCreate, AuditQuery, AuditListResponse)
    - api/src/tests/helpers/test-database.ts (TestDatabase)
    - api/src/tests/fixtures/identity-factory.ts (createIdentity)
  </read_first>
  <behavior>
    - Test 1: "should save and retrieve audit record with all required fields"
      - Create AuditRecordCreate with valid fields
      - Call auditService.save()
      - Query by identityId
      - Assert record contains: identityId, prompt, output, reasoningTrace, validationReport, timestamp, hash
    - Test 2: "should reject updates to existing audit records"
      - Save audit record
      - Attempt to update via direct MongoDB collection update
      - Assert no update endpoint exists in AuditService (only save and query)
    - Test 3: "should query audit records by date range"
      - Save 3 records with different timestamps
      - Query with from/to date range covering only 2 records
      - Assert exactly 2 records returned
    - Test 4: "should include SHA-256 hash for tamper detection"
      - Save record
      - Query record
      - Assert hash field exists and is 64 hex characters
  </behavior>
  <action>
    Create `api/src/tests/services/audit-integration.test.ts`:
    - Import { AuditService } from '../../services/audit-service'
    - Import { AuditRecordCreate } from '../../types/audit'
    - Import { createIdentity } from '../fixtures/identity-factory'
    - Use global.testDb for database connection
    - Create AuditService instance connected to test DB
    - Implement 4 test cases as specified in behavior
    - For test 2 (reject updates): verify AuditService has no update() or delete() method using reflection or type checking
  </action>
  <verify>
    <automated>cd api && npx jest src/tests/services/audit-integration.test.ts --passWithNoTests --detectOpenHandles --forceExit 2>&1 | head -30</automated>
  </verify>
  <done>
    - audit-integration.test.ts compiles and runs
    - 4 test cases cover: full fields, immutability (no update/delete), date range query, hash verification
    - Tests verify AuditService is append-only (no update/delete methods)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production data | Tests must only touch test database |
| mock → real provider | Mock provider must not call real LLM APIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | pipeline.integration.test.ts | mitigate | Use fake identities only; no real developer data |
| T-10-05 | Denial of Service | mock-provider-factory.ts | mitigate | Mock provider never calls external APIs |
| T-10-06 | Tampering | audit-integration.test.ts | accept | Tests verify immutability; tampering would fail tests |
</threat_model>

<verification>
- `npm test -- src/tests/pipeline.integration.test.ts` runs without TypeScript errors
- `npm test -- src/tests/services/audit-integration.test.ts` runs without TypeScript errors
- Test database helper resets between tests (no state leakage)
- Mock provider returns in <10ms (not calling real API)
</verification>

<success_criteria>
- Full pipeline test exercises: IdentityService → Orchestrator → AuditService in sequence
- Audit record verification checks all required fields from AUD-01 through AUD-04
- Identity binding latency is measured and asserted ≤200ms
- Memory scoping test proves no cross-identity leakage
- Audit immutability test proves no update/delete endpoints exist
</success_criteria>

<output>
After completion, create `.planning/phases/10-end-to-end-integration/10-02-SUMMARY.md`
</output>
