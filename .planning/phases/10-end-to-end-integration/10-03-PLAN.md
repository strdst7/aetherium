---
phase: 10-end-to-end-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - api/src/tests/consistency.integration.test.ts
  - api/src/tests/backward-compat.extended.test.ts
  - api/src/tests/cross-identity-leakage.test.ts
autonomous: true
requirements:
  - AG-01
  - AG-02
  - AG-07
  - AG-08
  - RE-01
  - RE-02
  - RE-03
  - RE-04
  - RE-06
  - MY-01
  - MY-02
  - MY-04
  - SH-01
  - SH-02
  - SH-05
  - AUD-01
  - AUD-04
must_haves:
  truths:
    - "Same prompt submitted twice returns outputs with identical tone, worldview, and symbolic anchors"
    - "All existing API contracts remain backward-compatible (additive changes only)"
    - "Cross-identity memory leakage test passes: memory from identity A is not visible to identity B"
    - "Cross-identity context contamination test passes: generation for identity B does not include identity A's constraints"
  artifacts:
    - path: "api/src/tests/consistency.integration.test.ts"
      provides: "Identity consistency verification tests"
      exports: []
    - path: "api/src/tests/backward-compat.extended.test.ts"
      provides: "Extended backward compatibility tests for all API versions"
      exports: []
    - path: "api/src/tests/cross-identity-leakage.test.ts"
      provides: "Cross-identity leakage and contamination tests"
      exports: []
  key_links:
    - from: "api/src/tests/consistency.integration.test.ts"
      to: "api/src/tests/helpers/mock-provider-factory.ts"
      via: "import { createIdentityAwareProvider }"
      pattern: "createIdentityAwareProvider"
    - from: "api/src/tests/cross-identity-leakage.test.ts"
      to: "api/src/tests/helpers/test-database.ts"
      via: "import { createTestDatabase }"
      pattern: "createTestDatabase"
---

<objective>
Verify identity consistency (deterministic outputs for same identity + prompt), extended backward compatibility across all API versions, and zero cross-identity leakage or contamination.

Purpose: These tests prove the core Aetherium value proposition — identity-consistent outputs with strict isolation. Backward compatibility ensures existing API consumers are not broken by v1 additions.
Output: Consistency tests, extended backward compatibility tests, leakage/contamination tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key prior art from STATE.md:
- MythicModule applies tone, voice, and symbolic context to outputs (Phase 6)
- IdentityBindingService enforces constraints (Phase 5)
- MemoryService scopes retrieval by identity_anchor (Phase 5)
- Backward compat tests exist in api/src/tests/backward-compat.test.ts (Phase 3)
- All API changes must be additive (critical constraint from AGENTS.md)
</context>

<interfaces>
From api/src/services/mythic-module.ts:
```typescript
export class MythicModule {
  generateSchema(identity: SigilIdentity): MythicIdentitySchema;
  generatePromptContext(identity: SigilIdentity): string;
  mythify(identity: SigilIdentity, rawOutput: string): string;
}
```

From api/src/types/api-contracts.ts:
```typescript
export interface ReasonResponse {
  id: string;
  output: string;
  status: "approved" | "refine" | "reject";
  identity_anchor: string;
  reasoning: {
    orchestrator: { selectedProvider: string; relevantMemoriesCount: number; topMemories: any[] };
    reflective: { status: string; violations: string[]; suggestedConstraints: string[]; confidenceScore: number };
    trace: Array<{ stage: string; timestamp: string; details: any }>;
  };
  apiVersion: string;
  toolCalls?: any[];
  toolResults?: any[];
  toolExecutionTrace?: any[];
  plan?: any;
  actions?: any[];
  planStatus?: string;
  validationReport?: ValidationReport;
  metadata?: any;
}
```

From api/src/tests/backward-compat.test.ts:
- Existing tests verify ReasonResponse field presence and optional fields (toolCalls, toolResults, plan, actions, planStatus)
- Status enum values: "approved", "refine", "reject"
- apiVersion field is required
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: implement identity consistency integration tests</name>
  <files>api/src/tests/consistency.integration.test.ts</files>
  <read_first>
    - api/src/services/orchestrator.ts (process method, context output)
    - api/src/services/mythic-module.ts (mythify, generatePromptContext)
    - api/src/services/identity-binding.ts (resolve method)
    - api/src/tests/fixtures/identity-factory.ts (createIdentity)
    - api/src/tests/helpers/mock-provider-factory.ts (createIdentityAwareProvider)
    - api/src/tests/backward-compat.test.ts (existing backward compat assertions)
    - api/src/types/api-contracts.ts (ReasonResponse shape)
  </read_first>
  <behavior>
    - Test 1: "should return consistent tone for same identity and prompt"
      - Create identity with voice="formal", tone="academic"
      - Submit identical prompt twice via orchestrator.process()
      - Assert both outputs contain formal/academic language markers
      - Assert outputs are structurally similar (same paragraph count or sentence structure)
    - Test 2: "should apply symbolic anchors consistently"
      - Create identity with specific symbolic anchors from design/sigil/v1.json
      - Submit identical prompt twice
      - Assert both outputs reference the same symbolic anchors
    - Test 3: "should maintain worldview consistency across multiple prompts"
      - Create identity with worldview="optimistic"
      - Submit 3 different prompts on same topic
      - Assert all 3 outputs reflect optimistic worldview
    - Test 4: "should produce identical validation reports for identical inputs"
      - Create identity with specific constraints
      - Submit identical prompt twice
      - Assert validationReport.ruleChecks are identical between runs
  </behavior>
  <action>
    Create `api/src/tests/consistency.integration.test.ts`:
    - Import Orchestrator, IdentityBindingService, MythicModule, SovereignHaloService
    - Import { createIdentity } from '../fixtures/identity-factory'
    - Import { createIdentityAwareProvider } from '../helpers/mock-provider-factory'
    - Import { createTestDatabase } from '../helpers/test-database'
    - Use global.testDb for database connection
    - For each test:
      1. Create identity with specific tone/voice/worldview configuration
      2. Setup mock provider that returns text incorporating identity characteristics
      3. Call orchestrator.process() with same request twice
      4. Assert consistency criteria
    - Define helper function `extractToneMarkers(text: string): string[]` that returns detected tone words
    - Define helper function `extractSymbolicReferences(text: string, anchors: string[]): string[]` that finds anchor mentions
    - Use expect.stringContaining() for tone marker assertions
    - Use expect.arrayContaining() for symbolic anchor assertions
  </action>
  <verify>
    <automated>cd api && npx jest src/tests/consistency.integration.test.ts --passWithNoTests --detectOpenHandles --forceExit 2>&1 | head -40</automated>
  </verify>
  <done>
    - consistency.integration.test.ts compiles and runs
    - 4 test cases cover: tone consistency, symbolic anchor consistency, worldview consistency, validation report consistency
    - Tests use createIdentityAwareProvider to simulate identity-aware LLM responses
  </done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement extended backward compatibility tests</name>
  <files>api/src/tests/backward-compat.extended.test.ts</files>
  <read_first>
    - api/src/tests/backward-compat.test.ts (existing backward compat tests — must preserve all)
    - api/src/types/api-contracts.ts (ReasonResponse, ReasonRequest, all types)
    - api/src/middleware/version-negotiation.ts (version negotiation logic)
    - api/openapi.yml (OpenAPI spec if exists)
  </read_first>
  <behavior>
    - Test 1: "should accept v1.0.0 ReasonRequest without optional fields"
      - Create ReasonRequest with only required fields (identity_anchor, messages)
      - Assert no TypeScript compilation errors
      - Assert validation passes
    - Test 2: "should accept v1.0.0 ReasonRequest with all Phase 1-3 optional fields"
      - Create ReasonRequest with toolCalls, toolResults, plan, actions, planStatus
      - Assert all fields are accepted without errors
    - Test 3: "should accept v1.0.0 ReasonRequest with Phase 4-9 optional fields"
      - Create ReasonRequest with validationReport, metadata, etc.
      - Assert backward compatibility (old consumers can ignore new fields)
    - Test 4: "should reject unknown API versions"
      - Test version negotiation with unsupported version (e.g., "99.0.0")
      - Assert 404 or appropriate error response
    - Test 5: "should maintain all existing endpoint paths and methods"
      - Verify POST /v1/reason exists
      - Verify GET /health exists
      - Verify GET /identity exists (from Phase 4)
      - Verify GET /audit exists (from Phase 8)
    - Test 6: "should not remove or rename any existing fields in ReasonResponse"
      - Verify all fields from backward-compat.test.ts still exist
      - Verify field types have not changed
  </behavior>
  <action>
    Create `api/src/tests/backward-compat.extended.test.ts`:
    - Import all API contract types from '../types/api-contracts'
    - Import existing backward-compat test cases as reference (do not duplicate — extend)
    - Test 1-3: Use TypeScript type-level assertions (compile-time checks):
      ```typescript
      const minimalRequest: ReasonRequest = { identity_anchor: "test", messages: [] };
      expect(minimalRequest.identity_anchor).toBe("test");
      ```
    - Test 4: Import version negotiation middleware and test directly:
      ```typescript
      import { versionNegotiation } from '../middleware/version-negotiation';
      // Mock req/res/next and test with unsupported version
      ```
    - Test 5: Import Express app or router and verify route registration:
      ```typescript
      import app from '../app'; // or relevant router
      const routes = app._router.stack.map(...);
      expect(routes).toContain('/v1/reason');
      ```
    - Test 6: Reflection-based check:
      ```typescript
      const response: ReasonResponse = { ... };
      const requiredFields = ['id', 'output', 'status', 'identity_anchor', 'reasoning', 'apiVersion'];
      requiredFields.forEach(field => {
        expect(response).toHaveProperty(field);
      });
      ```
    - Do NOT modify api/src/tests/backward-compat.test.ts — extend in the new file
  </action>
  <verify>
    <automated>cd api && npx jest src/tests/backward-compat.extended.test.ts --passWithNoTests --detectOpenHandles --forceExit 2>&1 | head -40</automated>
  </verify>
  <done>
    - backward-compat.extended.test.ts compiles and runs
    - 6 test cases covering: minimal request, full Phase 1-3 request, full Phase 4-9 request, version rejection, endpoint preservation, field preservation
    - Tests verify additive-only changes (no removed fields or renamed endpoints)
  </done>
</task>

<task type="auto" tdd="true">
  <name>task 3: implement cross-identity leakage and contamination tests</name>
  <files>api/src/tests/cross-identity-leakage.test.ts</files>
  <read_first>
    - api/src/services/memory-service.ts (vectorSearch signature with identity_anchor parameter)
    - api/src/services/identity-binding.ts (resolve method)
    - api/src/services/orchestrator.ts (process method, memory retrieval)
    - api/src/tests/fixtures/identity-factory.ts (createIdentity, createIdentityBatch)
    - api/src/tests/helpers/test-database.ts (TestDatabase)
  </read_first>
  <behavior>
    - Test 1: "should not leak memory shards between identities"
      - Create identityA and identityB
      - Upsert memory shard with identity_anchor = identityA.id
      - Query memory for identityB via MemoryService.vectorSearch
      - Assert results array is empty
    - Test 2: "should not leak memory shards in orchestrator context"
      - Create identityA with memory, identityB without memory
      - Call orchestrator.process() for identityB
      - Assert context.relevantMemories is empty
    - Test 3: "should not apply identity A constraints to identity B generation"
      - Create identityA with forbiddenBehaviors: ["use color red"]
      - Create identityB with no constraints
      - Register mock provider that returns text with "red"
      - Call orchestrator.process() for identityB
      - Assert validationReport.status is "approved" (not rejected by identityA's constraints)
    - Test 4: "should not mix identity contexts in multi-agent council"
      - Create identityA and identityB with different tones
      - Call MultiAgentOrchestrator.process() for identityB
      - Assert all council agent outputs match identityB's tone, not identityA's
    - Test 5: "should isolate audit records by identity"
      - Save audit record for identityA
      - Query audit for identityB
      - Assert results array is empty
  </behavior>
  <action>
    Create `api/src/tests/cross-identity-leakage.test.ts`:
    - Import MemoryService, IdentityBindingService, Orchestrator, MultiAgentOrchestrator
    - Import { createIdentity, createIdentityBatch } from './fixtures/identity-factory'
    - Import { createTestDatabase } from './helpers/test-database'
    - Import { MockProviderFactory } from './helpers/mock-provider-factory'
    - Use global.testDb for database connection
    - For each test:
      1. Create two distinct identities with different configurations
      2. Insert test data (memory, constraints) scoped to identityA
      3. Execute operation for identityB
      4. Assert no contamination or leakage
    - For Test 4 (multi-agent): 
      - Import MultiAgentOrchestrator from '../services/multi-agent-orchestrator'
      - Mock council agents if needed
      - Assert output contains identityB.name, not identityA.name
    - For Test 5 (audit isolation):
      - Use AuditService.save() for identityA
      - Query with identityB's identityId
      - Assert empty results
  </action>
  <verify>
    <automated>cd api && npx jest src/tests/cross-identity-leakage.test.ts --passWithNoTests --detectOpenHandles --forceExit 2>&1 | head -40</automated>
  </verify>
  <done>
    - cross-identity-leakage.test.ts compiles and runs
    - 5 test cases covering: memory shard leakage, orchestrator memory leakage, constraint contamination, multi-agent council contamination, audit record isolation
    - All tests use two distinct identities to prove strict isolation
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| identity A → identity B | No data or constraints must cross between identities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Information Disclosure | cross-identity-leakage.test.ts | mitigate | Tests verify zero leakage; failures indicate security bug |
| T-10-08 | Elevation of Privilege | cross-identity-leakage.test.ts | mitigate | Tests verify identity B cannot trigger identity A constraints |
| T-10-09 | Denial of Service | backward-compat.extended.test.ts | accept | Compatibility tests are read-only |
</threat_model>

<verification>
- All 3 test files compile and run with `npm test`
- Consistency tests prove deterministic identity-bound outputs
- Backward compatibility tests verify no breaking changes
- Leakage tests prove strict identity isolation (zero cross-identity data flow)
</verification>

<success_criteria>
- Identity consistency: same prompt + same identity = consistent tone, symbolic anchors, worldview
- Backward compatibility: all v1.0.0 API contracts work unchanged; all new fields are optional
- Cross-identity leakage: memory, constraints, audit records, and council outputs are strictly isolated
- No existing tests are broken by new integration tests
</success_criteria>

<output>
After completion, create `.planning/phases/10-end-to-end-integration/10-03-SUMMARY.md`
</output>
