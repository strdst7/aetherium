---
phase: 10-end-to-end-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/tests/fixtures/identity-factory.ts
  - api/src/tests/fixtures/pipeline-fixtures.ts
  - api/src/tests/helpers/test-database.ts
  - api/src/tests/helpers/mock-provider-factory.ts
  - api/jest.config.js
autonomous: true
requirements:
  - AG-01
  - AG-02
  - AG-03
  - AG-04
  - AG-05
  - AG-06
  - AG-07
  - AG-08
  - ID-01
  - ID-02
  - ID-03
  - ID-04
  - ID-05
  - ID-06
  - RE-01
  - RE-02
  - RE-03
  - RE-04
  - RE-05
  - RE-06
  - MY-01
  - MY-02
  - MY-03
  - MY-04
  - MY-05
  - SH-01
  - SH-02
  - SH-03
  - SH-04
  - SH-05
  - AUD-01
  - AUD-02
  - AUD-03
  - AUD-04
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
must_haves:
  truths:
    - "Test fixtures can generate valid SigilIdentity objects with configurable fields"
    - "Test fixtures can create mock LLM provider responses with identity-bound content"
    - "A test database helper can spin up an isolated MongoDB test instance"
    - "Mock provider factory supports deterministic and randomized response modes"
  artifacts:
    - path: "api/src/tests/fixtures/identity-factory.ts"
      provides: "Factory for creating test SigilIdentity objects"
      exports: ["createIdentity", "createIdentityBatch", "DEFAULT_TEST_IDENTITY"]
    - path: "api/src/tests/fixtures/pipeline-fixtures.ts"
      provides: "Fixtures for full pipeline test scenarios"
      exports: ["createPipelineScenario", "PipelineScenario"]
    - path: "api/src/tests/helpers/test-database.ts"
      provides: "Isolated MongoDB test instance management"
      exports: ["TestDatabase", "createTestDatabase"]
    - path: "api/src/tests/helpers/mock-provider-factory.ts"
      provides: "Configurable mock LLM provider for tests"
      exports: ["MockProviderFactory", "createDeterministicProvider"]
  key_links:
    - from: "api/src/tests/fixtures/pipeline-fixtures.ts"
      to: "api/src/tests/fixtures/identity-factory.ts"
      via: "import { createIdentity }"
      pattern: "createIdentity"
    - from: "api/src/tests/helpers/mock-provider-factory.ts"
      to: "api/src/adapters/ai-adapter.ts"
      via: "implements AIProvider interface"
      pattern: "AIProvider"
---

<objective>
Build shared test infrastructure for Phase 10: reusable fixtures, mock factories, and test database helpers that all integration and E2E tests depend on.

Purpose: Without shared fixtures, every integration test would duplicate identity creation, mock setup, and database wiring. Centralizing these reduces test code volume and ensures consistency across the pipeline, consistency, and provider E2E test suites.
Output: Factory modules, test database helper, mock provider factory, updated jest config.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key prior art from STATE.md:
- IdentityService with MongoDB persistence (Phase 4)
- Orchestrator with identity binding, mythic module, sovereign halo (Phases 5-7)
- AuditService with append-only records (Phase 8)
- MockAgentBuilder and MockMCPServer exist (Phase 1-2)
- All prior API contracts must remain backward-compatible (additive changes only)
</context>

<interfaces>
From api/src/types/identity.ts:
```typescript
export interface SigilIdentity {
  id: string;
  name: string;
  developerId: string;
  sigilHash: string;
  version: number;
  config?: IdentityConfig;
  versions: IdentityVersion[];
  createdAt: string;
  updatedAt: string;
}
```

From api/src/adapters/ai-adapter.ts:
```typescript
export interface AIProvider {
  generate(prompt: string, options?: GenerateOptions): Promise<GenerateResult>;
  embed?(text: string): Promise<EmbedResult>;
}
```

From api/src/services/audit-service.ts:
```typescript
export class AuditService {
  constructor(uri?: string);
  async connect(uri?: string): Promise<void>;
  async save(record: AuditRecordCreate): Promise<AuditRecord | null>;
  async query(query: AuditQuery): Promise<AuditListResponse>;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: create identity and pipeline fixture factories</name>
  <files>api/src/tests/fixtures/identity-factory.ts, api/src/tests/fixtures/pipeline-fixtures.ts</files>
  <read_first>
    - api/src/types/identity.ts (SigilIdentity shape)
    - api/src/types/api-contracts.ts (ReasonRequest, ReasonResponse shapes)
    - api/src/types/audit.ts (AuditRecordCreate shape)
    - api/src/services/identity-service.ts (IdentityService.create method signature)
  </read_first>
  <action>
    1. Create `api/src/tests/fixtures/identity-factory.ts`:
       - Export `createIdentity(overrides?: Partial<SigilIdentity>): SigilIdentity` that returns a valid identity with:
         - id: `id_${nanoid(8)}` or `test-id-${Date.now()}`
         - name: "Test Identity"
         - developerId: "test@example.com"
         - sigilHash: deterministic hash of name + developerId (use simple hash, e.g., `createHash('sha256').update(...).digest('hex').slice(0, 16)`)
         - version: 1
         - config: { preferredProvider: "gemini", customRules: ["Be helpful"] }
         - versions: []
         - createdAt: new Date().toISOString()
         - updatedAt: new Date().toISOString()
       - Export `createIdentityBatch(count: number): SigilIdentity[]`
       - Export `DEFAULT_TEST_IDENTITY: SigilIdentity` (frozen constant)
       - All fields must match the SigilIdentity interface exactly.

    2. Create `api/src/tests/fixtures/pipeline-fixtures.ts`:
       - Export `PipelineScenario` interface:
         ```typescript
         interface PipelineScenario {
           name: string;
           identity: SigilIdentity;
           request: ReasonRequest;
           expectedOutputContains?: string[]; // strings that must appear in output
           expectedTone?: string;
           expectedAuditFields?: (keyof AuditRecord)[];
         }
         ```
       - Export `createPipelineScenario(name: string, overrides?: Partial<PipelineScenario>): PipelineScenario`
       - Provide 3 built-in scenarios:
         - "neutral_identity": uses DEFAULT_TEST_IDENTITY, simple prompt "Explain quantum computing"
         - "mythic_identity": identity with voice="formal", tone="academic", symbolic anchors
         - "constrained_identity": identity with forbidden behaviors ["no jokes", "no slang"]
       - Import `createIdentity` and `DEFAULT_TEST_IDENTITY` from `./identity-factory`
  </action>
  <verify>
    <automated>cd api && npx tsc --noEmit src/tests/fixtures/identity-factory.ts src/tests/fixtures/pipeline-fixtures.ts</automated>
  </verify>
  <done>
    - `identity-factory.ts` compiles and exports createIdentity, createIdentityBatch, DEFAULT_TEST_IDENTITY
    - `pipeline-fixtures.ts` compiles and exports PipelineScenario, createPipelineScenario with 3 built-in scenarios
    - Both files have zero TypeScript errors
  </done>
</task>

<task type="auto">
  <name>task 2: create test database helper and mock provider factory</name>
  <files>api/src/tests/helpers/test-database.ts, api/src/tests/helpers/mock-provider-factory.ts</files>
  <read_first>
    - api/src/services/audit-service.ts (AuditService constructor and connect signature)
    - api/src/adapters/ai-adapter.ts (AIProvider, GenerateOptions, GenerateResult interfaces)
    - api/src/adapters/mock-provider.ts (existing mock provider for patterns)
    - api/jest.config.js (current jest configuration)
  </read_first>
  <action>
    1. Create `api/src/tests/helpers/test-database.ts`:
       - Export `TestDatabase` class:
         - `constructor()` stores `uri` from `process.env.MONGODB_URI` or default `mongodb://localhost:27017/aetherium_test`
         - `async setup(): Promise<void>` — connects to MongoDB, drops existing test collections (audit_records, identities, memory_shards), creates indexes
         - `async teardown(): Promise<void>` — drops test collections, closes connection
         - `async reset(): Promise<void>` — drops + recreates collections (fast cleanup between tests)
         - `getDb(): Db` — returns MongoDB Db instance
       - Export `createTestDatabase(): TestDatabase` factory
       - Use MongoDB native driver (mongodb package), NOT Mongoose

    2. Create `api/src/tests/helpers/mock-provider-factory.ts`:
       - Export `MockProviderFactory` class implementing AIProvider:
         - `constructor(options: { deterministic?: boolean; responseText?: string; embedResult?: number[] })`
         - `async generate(prompt: string, options?: GenerateOptions): Promise<GenerateResult>`:
           - If deterministic=true, always returns `responseText` (default: "Mock generated response")
           - If deterministic=false, returns randomized text with configurable seed
           - Must return object with `{ text: string, usage?: { promptTokens: number, completionTokens: number } }`
         - `async embed(text: string): Promise<EmbedResult>`:
           - Returns fixed embedResult (default: 384-dimensional array of 0.1s) or deterministic hash-based embedding
         - Export `createDeterministicProvider(responseText: string): MockProviderFactory`
         - Export `createIdentityAwareProvider(identity: SigilIdentity): MockProviderFactory` — returns provider that includes identity.name in generated text (for consistency testing)

    3. Update `api/jest.config.js`:
       - Add `moduleNameMapper` if not present: `^@tests/(.*)$: '<rootDir>/src/tests/$1'`
       - Add `setupFilesAfterEnv: ['<rootDir>/src/tests/setup.ts']` (will be created in 10-02)
       - Ensure `testPathIgnorePatterns` excludes any real-provider tests from default run (pattern: `.*\.real\.test\.ts$`)
  </action>
  <verify>
    <automated>cd api && npx tsc --noEmit src/tests/helpers/test-database.ts src/tests/helpers/mock-provider-factory.ts</automated>
  </verify>
  <done>
    - `test-database.ts` compiles and exports TestDatabase with setup/teardown/reset methods
    - `mock-provider-factory.ts` compiles and exports MockProviderFactory, createDeterministicProvider, createIdentityAwareProvider
    - `api/jest.config.js` has moduleNameMapper for @tests/*, setupFilesAfterEnv, and ignores *.real.test.ts
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production database | Test code must NEVER connect to production MongoDB URI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | test-database.ts | mitigate | Drop collections on teardown; never write to production DB |
| T-10-02 | Information Disclosure | identity-factory.ts | accept | Test identities use fake data; no PII |
| T-10-03 | Denial of Service | mock-provider-factory.ts | accept | Mock provider is local only; no external calls |
</threat_model>

<verification>
- All fixture files compile with `tsc --noEmit`
- `npm test -- --listTests` in api/ shows new helper files are not detected as test files (they should not match test patterns)
- jest config is valid JSON/JS
</verification>

<success_criteria>
- Test fixtures produce valid SigilIdentity objects matching the real interface
- Test database helper can connect, reset, and teardown without errors
- Mock provider factory returns deterministic responses for consistency testing
- jest.config.js supports @tests/* imports and ignores real-provider tests by default
</success_criteria>

<output>
After completion, create `.planning/phases/10-end-to-end-integration/10-01-SUMMARY.md`
</output>
