---
phase: 10-end-to-end-integration
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - api/src/tests/real-provider.e2e.test.ts
  - api/src/tests/helpers/real-provider-guard.ts
  - api/src/tests/helpers/env-checker.ts
  - api/.env.test.example
autonomous: true
requirements:
  - AG-01
  - AG-02
  - AG-06
  - AG-07
  - AG-08
  - RE-01
  - RE-06
  - MY-01
  - MY-02
  - SH-01
  - SH-02
  - AUD-01
  - AUD-02
must_haves:
  truths:
    - "E2E tests can run against real Gemini / Vertex AI providers when credentials are available"
    - "Tests gracefully skip when real provider credentials are missing (no failures in CI)"
    - "Real provider E2E test validates full pipeline with actual LLM generation"
    - "Environment checker utility validates all required env vars before real provider tests run"
  artifacts:
    - path: "api/src/tests/real-provider.e2e.test.ts"
      provides: "End-to-end tests against real Gemini/Vertex AI"
      exports: []
    - path: "api/src/tests/helpers/real-provider-guard.ts"
      provides: "Jest conditional skip helper for real provider tests"
      exports: ["guardRealProviderTests"]
    - path: "api/src/tests/helpers/env-checker.ts"
      provides: "Environment variable validation utility"
      exports: ["checkRequiredEnvVars"]
    - path: "api/.env.test.example"
      provides: "Example environment variables for real provider tests"
      exports: []
  key_links:
    - from: "api/src/tests/real-provider.e2e.test.ts"
      to: "api/src/tests/helpers/real-provider-guard.ts"
      via: "import { guardRealProviderTests }"
      pattern: "guardRealProviderTests"
    - from: "api/src/tests/real-provider.e2e.test.ts"
      to: "api/src/tests/helpers/env-checker.ts"
      via: "import { checkRequiredEnvVars }"
      pattern: "checkRequiredEnvVars"
    - from: "api/src/tests/real-provider.e2e.test.ts"
      to: "api/src/adapters/gemini-provider.ts"
      via: "import { GeminiProvider }"
      pattern: "GeminiProvider"
---

<objective>
Create end-to-end tests that exercise the complete pipeline against REAL Gemini / Vertex AI providers, with graceful skipping when credentials are unavailable.

Purpose: Mock tests prove integration logic; real provider tests prove the system works with actual LLM generation, mythic shaping, and sovereign validation. The guard utilities ensure CI doesn't fail when credentials are absent.
Output: Real provider E2E test, guard utilities, env checker, example env file.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key prior art from STATE.md:
- GeminiProvider exists in api/src/adapters/gemini-provider.ts (Phase 1)
- ProviderRegistry supports multiple providers with failover (Phase 1)
- Real provider tests must be optional (not break CI without credentials)
- AGENTS.md specifies: "Gemini / Google Cloud credentials (to be added for v1)"
</context>

<interfaces>
From api/src/adapters/gemini-provider.ts:
```typescript
export class GeminiProvider implements AIProvider {
  constructor(apiKey?: string, projectId?: string);
  async generate(prompt: string, options?: GenerateOptions): Promise<GenerateResult>;
  async embed(text: string): Promise<EmbedResult>;
}
```

From api/src/services/provider-registry.ts:
```typescript
export class ProviderRegistry {
  register(name: string, provider: AIProvider, priority?: number): void;
  async pick(options?: ProviderPickOptions): Promise<AIProvider>;
}
```

From api/src/types/api-contracts.ts:
```typescript
export const CURRENT_API_VERSION = "1.0.0";
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: create environment checker and real provider guard utilities</name>
  <files>api/src/tests/helpers/env-checker.ts, api/src/tests/helpers/real-provider-guard.ts, api/.env.test.example</files>
  <read_first>
    - api/src/adapters/gemini-provider.ts (constructor signature — what env vars it needs)
    - api/jest.config.js (test environment and ignore patterns)
    - api/.env.local (if exists, to see current env var patterns)
  </read_first>
  <action>
    1. Create `api/src/tests/helpers/env-checker.ts`:
       - Export `RequiredEnvVar` interface: `{ name: string; description: string; validate?: (value: string) => boolean }`
       - Export `checkRequiredEnvVars(vars: RequiredEnvVar[]): { present: string[]; missing: string[] }`
       - Return object with `allPresent: boolean` and detailed status for each var
       - Example validation: check GOOGLE_API_KEY looks like a valid key (non-empty, min length 10)

    2. Create `api/src/tests/helpers/real-provider-guard.ts`:
       - Export `guardRealProviderTests(): { shouldRun: boolean; reason?: string }`
       - Check for `GOOGLE_API_KEY` or `GOOGLE_CLOUD_PROJECT` env vars
       - If missing, return `shouldRun: false` with reason string
       - Export `describeIfRealProvider(name: string, fn: () => void): void` — wrapper that only runs describe block if shouldRun is true
       - Export `itIfRealProvider(name: string, fn: () => void): void` — wrapper for individual tests
       - These must be Jest-compatible (use jest globals)

    3. Create `api/.env.test.example`:
       - Add comment header: "# Copy to .env.test and fill in real values to run E2E tests against Gemini"
       - Add `GOOGLE_API_KEY=your_api_key_here`
       - Add `GOOGLE_CLOUD_PROJECT=your_project_id`
       - Add `MONGODB_URI=mongodb://localhost:27017/aetherium_test`
       - Add `REDIS_URL=redis://localhost:6379`
       - Add comment: "# Optional: set to 'true' to force real provider tests even without credentials (will fail)"
       - Add `FORCE_REAL_PROVIDER_TESTS=false`
  </action>
  <verify>
    <automated>cd api && npx tsc --noEmit src/tests/helpers/env-checker.ts src/tests/helpers/real-provider-guard.ts</automated>
  </verify>
  <done>
    - env-checker.ts compiles and exports checkRequiredEnvVars
    - real-provider-guard.ts compiles and exports guardRealProviderTests, describeIfRealProvider, itIfRealProvider
    - .env.test.example exists with all required environment variables documented
  </done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement real provider E2E tests</name>
  <files>api/src/tests/real-provider.e2e.test.ts</files>
  <read_first>
    - api/src/adapters/gemini-provider.ts (GeminiProvider constructor and generate method)
    - api/src/services/provider-registry.ts (ProviderRegistry.register and pick)
    - api/src/services/orchestrator.ts (Orchestrator.process)
    - api/src/services/identity-service.ts (IdentityService.create)
    - api/src/services/audit-service.ts (AuditService.save, query)
    - api/src/tests/helpers/real-provider-guard.ts (describeIfRealProvider)
    - api/src/tests/helpers/env-checker.ts (checkRequiredEnvVars)
    - api/src/tests/fixtures/identity-factory.ts (createIdentity)
    - api/src/tests/helpers/test-database.ts (TestDatabase)
  </read_first>
  <behavior>
    - Test 1: "should connect to real Gemini and generate text"
      - Skip if GOOGLE_API_KEY is missing
      - Instantiate GeminiProvider with real credentials
      - Call generate() with simple prompt
      - Assert response.text is non-empty and has reasonable length (>10 chars)
    - Test 2: "should run full pipeline with real Gemini provider"
      - Skip if credentials missing
      - Create identity via IdentityService
      - Register GeminiProvider in ProviderRegistry
      - Call orchestrator.process() with identity_anchor
      - Assert response.text is non-empty
      - Assert response.validationReport exists
      - Assert audit record exists in database
    - Test 3: "should verify identity shaping with real provider"
      - Skip if credentials missing
      - Create identity with formal tone and academic voice
      - Call orchestrator.process()
      - Assert output reflects formal tone (contains formal language markers)
    - Test 4: "should handle real provider failures gracefully"
      - Skip if credentials missing
      - Temporarily use invalid API key
      - Assert ProviderRegistry.failover picks next provider or returns structured error
      - Restore valid credentials after test
  </behavior>
  <action>
    Create `api/src/tests/real-provider.e2e.test.ts`:
    - Import { describeIfRealProvider, itIfRealProvider } from './helpers/real-provider-guard'
    - Import { checkRequiredEnvVars } from './helpers/env-checker'
    - Import { GeminiProvider } from '../adapters/gemini-provider'
    - Import { ProviderRegistry } from '../services/provider-registry'
    - Import { Orchestrator } from '../services/orchestrator'
    - Import { IdentityService } from '../services/identity-service'
    - Import { AuditService } from '../services/audit-service'
    - Import { createIdentity } from './fixtures/identity-factory'
    - Use describeIfRealProvider() for all test blocks
    - In beforeAll:
      1. Check env vars via checkRequiredEnvVars
      2. Connect test database via global.testDb
      3. Log "[Real Provider E2E] Credentials present, running against live Gemini"
    - In afterAll:
      1. Disconnect test database
      2. Log "[Real Provider E2E] Tests complete"
    - Test 1: Direct GeminiProvider test (no Orchestrator)
    - Test 2: Full Orchestrator pipeline with real provider
    - Test 3: Identity shaping verification (tone/voice)
    - Test 4: Failover test with invalid key
    - Set timeout to 30000ms for real provider tests (slow network calls)
  </action>
  <verify>
    <automated>cd api && npx jest src/tests/real-provider.e2e.test.ts --passWithNoTests --detectOpenHandles --forceExit 2>&1 | head -30</automated>
  </verify>
  <done>
    - real-provider.e2e.test.ts compiles and runs
    - All tests wrapped in describeIfRealProvider / itIfRealProvider
    - Tests skip gracefully when GOOGLE_API_KEY is missing (returns with 0 failures)
    - Tests have 30-second timeout for real network calls
    - jest.config.js ignores *.e2e.test.ts by default (separate from *.real.test.ts pattern)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → real LLM API | Real provider tests call external APIs with credentials |
| CI → secrets | CI must not fail when secrets are absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Information Disclosure | real-provider.e2e.test.ts | mitigate | Never log API keys; use env vars only |
| T-10-11 | Denial of Service | real-provider.e2e.test.ts | mitigate | 30s timeout prevents hanging tests; skip if no credentials |
| T-10-12 | Elevation of Privilege | .env.test.example | accept | Example file contains no real credentials |
</threat_model>

<verification>
- `npm test -- src/tests/real-provider.e2e.test.ts` skips all tests when GOOGLE_API_KEY is missing
- `GOOGLE_API_KEY=fake-key npm test -- src/tests/real-provider.e2e.test.ts` attempts to run (will likely fail due to invalid key, proving guard works)
- env-checker correctly identifies missing vs present variables
</verification>

<success_criteria>
- Real provider E2E tests run against Gemini when credentials are available
- Tests skip gracefully (no failures) when credentials are missing
- Environment checker validates all required variables
- Example env file documents all needed configuration
- Real provider tests have appropriate timeouts for network calls
</success_criteria>

<output>
After completion, create `.planning/phases/10-end-to-end-integration/10-04-SUMMARY.md`
</output>
