---
phase: 09-web-ui-extensions
plan: 06
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-03
  - 09-04
files_modified:
  - web/pages/index.tsx
autonomous: true
requirements:
  - UI-02
  - UI-06
must_haves:
  truths:
    - "Identity Test page uses IdentitySelector dropdown instead of free-text input"
    - "Page submits prompt and displays output, reasoning trace, and validation report"
    - "Active identity selection persists across navigation"
  artifacts:
    - path: "web/pages/index.tsx"
      provides: "Refactored Identity Test page"
  key_links:
    - from: "web/pages/index.tsx"
      to: "web/components/IdentitySelector.tsx"
      via: "import { IdentitySelector }"
    - from: "web/pages/index.tsx"
      to: "web/components/ReasoningTrace.tsx"
      via: "import { ReasoningTrace }"
    - from: "web/pages/index.tsx"
      to: "web/components/ValidationReport.tsx"
      via: "import { ValidationReport }"
---

<objective>
Refactor the existing reasoning shell (`/`) into the Identity Test page (UI-02) by replacing the free-text identity input with the shared `IdentitySelector`, integrating the new `ReasoningTrace` and `ValidationReport` components, and wiring the active identity to global context (UI-06).

Purpose: The index page is the primary interaction point. It must demonstrate the full identity-bound pipeline: select identity → submit prompt → see output + trace + validation.
Output: Updated `web/pages/index.tsx`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@web/pages/index.tsx
@web/components/IdentitySelector.tsx
@web/components/ReasoningTrace.tsx
@web/components/ValidationReport.tsx
@web/src/types/api.ts
@web/src/lib/api-client.ts
@web/src/lib/identity-context.tsx
</context>

<tasks>

<task type="auto">
  <name>task 1: refactor index.tsx into the Identity Test page</name>
  <files>web/pages/index.tsx</files>
  <read_first>
    - web/pages/index.tsx (current implementation to preserve existing logic)
    - web/components/IdentitySelector.tsx
    - web/components/ReasoningTrace.tsx
    - web/components/ValidationReport.tsx
    - web/src/types/api.ts (ReasoningResponse with validationReport)
    - web/src/lib/api-client.ts (submitReasonRequest)
    - web/src/lib/identity-context.tsx (to be created in Plan 07, but we import it here)
  </read_first>
  <action>
    Refactor `web/pages/index.tsx`:
    1. **Remove local `ReasoningResponse` interface** and import `ReasoningResponse` from `../src/types/api`.
    2. **Replace identity input**:
       - Remove the `<input>` for `identityAnchor`.
       - Import `IdentitySelector` and render it with `value={identityAnchor} onChange={setIdentityAnchor}`.
       - If `identity-context.tsx` exists (from Plan 07), import `useIdentity` and sync `identityAnchor` with the global context. If not yet available, leave a TODO comment and use local state.
    3. **Update API call**:
       - Replace the inline `fetch` with `submitReasonRequest(identityAnchor, messages, options)` from `web/src/lib/api-client.ts`.
    4. **Enhance response display**:
       - Keep the output text display.
       - **Replace inline trace rendering** with `<ReasoningTrace trace={response.reasoning.trace} />`.
       - **Add ValidationReport**: if `response.validationReport` exists, render `<ValidationReport report={response.validationReport} />` before or alongside the reflective result.
       - Keep the existing orchestrator context and reflective result display as supplementary info (do not remove them to maintain backward compatibility of the UI experience).
    5. **Update page metadata**:
       - Change `<title>` to "Aetherium | Identity Test".
       - Update heading to "Identity Test".
    6. **Preserve existing functionality**:
       - Keep demo prompts, error handling, loading states, focus management, and provider failure simulation buttons.
       - Keep inline styles (no external CSS).
  </action>
  <acceptance_criteria>
    - `web/pages/index.tsx` imports `ReasoningResponse` from `../src/types/api` (local interface removed)
    - `web/pages/index.tsx` imports `IdentitySelector` from `../components/IdentitySelector`
    - `web/pages/index.tsx` imports `ReasoningTrace` from `../components/ReasoningTrace`
    - `web/pages/index.tsx` imports `ValidationReport` from `../components/ValidationReport`
    - `web/pages/index.tsx` imports `submitReasonRequest` from `../src/lib/api-client`
    - No local `interface ReasoningResponse` remains in `web/pages/index.tsx`
    - The page renders `<ReasoningTrace trace={...} />` and `<ValidationReport report={...} />` when response is present
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "import { ReasoningResponse }" web/pages/index.tsx && \
      grep -c "import { IdentitySelector }" web/pages/index.tsx && \
      grep -c "import { ReasoningTrace }" web/pages/index.tsx && \
      grep -c "import { ValidationReport }" web/pages/index.tsx && \
      grep -c "import { submitReasonRequest }" web/pages/index.tsx && \
      grep -c "<ReasoningTrace" web/pages/index.tsx && \
      grep -c "<ValidationReport" web/pages/index.tsx && \
      grep -vc "interface ReasoningResponse {" web/pages/index.tsx
    </automated>
  </verify>
  <done>Index page uses the shared identity selector, submits via the API client, and displays reasoning trace and validation report components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → API | Prompt and identity cross to backend. No additional risk beyond existing POST /v1/reason. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Information Disclosure | ValidationReport display | accept | Report belongs to the user who generated it. |
| T-09-14 | Denial of Service | large trace/validation arrays | mitigate | Components cap rendered items (ValidationReport already capped in Plan 04; ReasoningTrace is simple list). |
</threat_model>

<verification>
- `cd web && npx tsc --noEmit` compiles after changes.
- Page loads at `/` and successfully submits a prompt using the dropdown identity.
- Response section shows output, trace, and validation report.
</verification>

<success_criteria>
- Identity Test page uses `IdentitySelector` for identity input.
- Submitting a prompt returns output, reasoning trace (via `ReasoningTrace`), and validation report (via `ValidationReport`).
- All existing interactive features (demo prompts, provider failure simulation) continue to work.
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-ui-extensions/09-06-SUMMARY.md`
</output>
