---
phase: 09-web-ui-extensions
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - web/components/ReasoningTrace.tsx
autonomous: true
requirements:
  - UI-03
must_haves:
  truths:
    - "ReasoningTrace component accepts an array of trace steps"
    - "Each step is rendered as an expandable card with stage name, timestamp, and details"
    - "Component is styled consistently with the design system"
  artifacts:
    - path: "web/components/ReasoningTrace.tsx"
      provides: "Expandable reasoning trace display"
      exports: ["ReasoningTrace"]
  key_links:
    - from: "web/components/ReasoningTrace.tsx"
      to: "web/src/types/api.ts"
      via: "import { ReasoningTrace as TraceStep }"
---

<objective>
Build a reusable `ReasoningTrace` component that renders the reasoning trace from an API response in an expandable, step-by-step format (UI-03).

Purpose: The trace is critical for debugging identity-bound reasoning. A reusable component keeps the display consistent across pages.
Output: `web/components/ReasoningTrace.tsx`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@web/src/types/api.ts
@design/sigil/v1.json
</context>

<tasks>

<task type="auto">
  <name>task 1: implement ReasoningTrace component</name>
  <files>web/components/ReasoningTrace.tsx</files>
  <read_first>
    - web/src/types/api.ts (ReasoningTrace type shape)
    - web/pages/index.tsx (current trace rendering for style reference)
    - design/sigil/v1.json (tokens)
  </read_first>
  <action>
    Create `web/components/ReasoningTrace.tsx`:
    - Props:
      ```typescript
      interface ReasoningTraceProps {
        trace: Array<{
          stage: string;
          timestamp: string;
          details: any;
        }>;
      }
      ```
    - State: `expandedSteps: Set<number>` (or boolean array) to track which steps are open.
    - Render a container with heading "Processing Trace".
    - For each step in `trace`:
      - Render a card (white background, 4px border-radius, 12px padding, margin-bottom 8px).
      - Header row (clickable): left side shows `stage` name in bold primary color, right side shows formatted `timestamp` (use `new Date(timestamp).toLocaleTimeString()`).
      - Chevron indicator: ▶ when collapsed, ▼ when expanded.
      - Expanded body: render `details` as `<pre>` with `JSON.stringify(details, null, 2)` inside a scrollable container (`overflow: 'auto'`, maxHeight 300px, background neutral[100], padding 12px, fontFamily mono).
    - If `trace` is empty, render "No trace available."
    - Styles: use tokens for colors, spacing, typography.
  </action>
  <acceptance_criteria>
    - `web/components/ReasoningTrace.tsx` exports `ReasoningTrace`
    - Component defines `ReasoningTraceProps` with `trace: Array<{ stage: string; timestamp: string; details: any }>`
    - Component uses `useState` for expanded step state
    - Clicking a step toggles visibility of its details
    - Details are rendered inside `<pre>` with `JSON.stringify(details, null, 2)`
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "export function ReasoningTrace" web/components/ReasoningTrace.tsx && \
      grep -c "useState" web/components/ReasoningTrace.tsx && \
      grep -c "JSON.stringify" web/components/ReasoningTrace.tsx && \
      grep -c "stage" web/components/ReasoningTrace.tsx && \
      grep -c "timestamp" web/components/ReasoningTrace.tsx
    </automated>
  </verify>
  <done>Component renders a list of expandable trace steps matching the design system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered trace data | `details` may contain arbitrary JSON from the server. XSS risk if rendered as HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Information Disclosure | ReasoningTrace | accept | Trace data is already exposed to the user who generated it. |
| T-09-06 | Tampering | ReasoningTrace | accept | Display-only component; no user input. |
| T-09-07 | XSS | ReasoningTrace details | mitigate | Render `details` inside `<pre>` using `JSON.stringify`, not `dangerouslySetInnerHTML`. Never render raw HTML from trace details. |
</threat_model>

<verification>
- Component renders in isolation (Storybook or test) with a sample trace array.
- Expansion/collapse toggles correctly.
</verification>

<success_criteria>
- `ReasoningTrace` accepts a `trace` prop and renders each step.
- Steps are expandable and show formatted JSON details.
- Styling matches the Sigil design system tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-ui-extensions/09-03-SUMMARY.md`
</output>
