---
phase: 09-web-ui-extensions
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - web/components/IdentityForm.tsx
  - web/pages/identity/register.tsx
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "Developer can navigate to /identity/register and see a form"
    - "Form submits to POST /identity/register and creates a new SigilIdentity"
    - "Validation errors from the API are displayed inline"
  artifacts:
    - path: "web/pages/identity/register.tsx"
      provides: "Identity registration page"
    - path: "web/components/IdentityForm.tsx"
      provides: "Reusable identity form component"
      exports: ["IdentityForm"]
  key_links:
    - from: "web/pages/identity/register.tsx"
      to: "web/components/IdentityForm.tsx"
      via: "import { IdentityForm }"
    - from: "web/components/IdentityForm.tsx"
      to: "web/src/lib/api-client.ts"
      via: "import { registerIdentity }"
---

<objective>
Build the Identity Registration page (UI-01) where a developer can create a new SigilIdentity via a form.

Purpose: This is the primary identity management interface for v1.
Output: `IdentityForm` component and `/identity/register` page.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@web/src/types/api.ts
@web/src/lib/api-client.ts
@web/components/Layout.tsx
@api/src/types/identity.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: create IdentityForm component</name>
  <files>web/components/IdentityForm.tsx</files>
  <read_first>
    - web/src/types/api.ts (IdentityCreateRequest shape)
    - web/src/lib/api-client.ts (registerIdentity signature)
    - api/src/types/identity.ts (server-side source of truth for fields)
  </read_first>
  <action>
    Create `web/components/IdentityForm.tsx`:
    - Props: `onSuccess?: (identity: SigilIdentity) => void`
    - State: form fields + loading + errors + success.
    - Fields (matching `IdentityCreateRequest`):
      - `name` (text input, required)
      - `developerId` (text input, required)
      - `voice` (textarea)
      - `constraints` (textarea)
      - `mythicSignature` (text input)
      - `allowedBehaviors` (textarea, comma-separated or multiline)
      - `forbiddenBehaviors` (textarea, comma-separated or multiline)
    - On submit:
      - Call `registerIdentity(payload)`.
      - If error (AetheriumError with validation errors), display inline errors per field.
      - If success, show success banner with created `identity_id` and `sigil_hash`, then call `onSuccess`.
    - Styles: use `web/src/tokens.ts` for consistent form styling (padding, border, border-radius, shadows).
  </action>
  <acceptance_criteria>
    - `web/components/IdentityForm.tsx` exports `IdentityForm`
    - Form contains input elements with `name` attribute or state keys for: `name`, `developerId`, `voice`, `constraints`, `mythicSignature`, `allowedBehaviors`, `forbiddenBehaviors`
    - `registerIdentity` is imported from `../src/lib/api-client`
    - On successful submission, component displays `identity_id` and `sigil_hash` of the created identity
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "export function IdentityForm" web/components/IdentityForm.tsx && \
      grep -c "registerIdentity" web/components/IdentityForm.tsx && \
      grep -c "name" web/components/IdentityForm.tsx && \
      grep -c "developerId" web/components/IdentityForm.tsx && \
      grep -c "sigil_hash" web/components/IdentityForm.tsx
    </automated>
  </verify>
  <done>Form component renders, validates client-side for required fields, submits to API, and surfaces server validation errors.</done>
</task>

<task type="auto">
  <name>task 2: create identity registration page</name>
  <files>web/pages/identity/register.tsx</files>
  <read_first>
    - web/components/Layout.tsx
    - web/components/IdentityForm.tsx
  </read_first>
  <action>
    Create `web/pages/identity/register.tsx`:
    - Use `Layout` as wrapper.
    - Page heading: "Register Sigil Identity".
    - Subheading: "Define a new identity with voice, constraints, and behavioral boundaries."
    - Render `<IdentityForm onSuccess={(identity) => { /* could redirect or show link */ }} />`.
    - Below the form, add a link back to home (`<a href="/">← Back to Identity Test</a>`).
    - Ensure page is exported as default.
  </action>
  <acceptance_criteria>
    - `web/pages/identity/register.tsx` exports default page component
    - Imports `Layout` from `../../components/Layout`
    - Imports `IdentityForm` from `../../components/IdentityForm`
    - Contains text "Register Sigil Identity"
    - Contains a link back to "/"
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "export default" web/pages/identity/register.tsx && \
      grep -c "Layout" web/pages/identity/register.tsx && \
      grep -c "IdentityForm" web/pages/identity/register.tsx && \
      grep -c "Register Sigil Identity" web/pages/identity/register.tsx && \
      grep -c 'href="/"' web/pages/identity/register.tsx
    </automated>
  </verify>
  <done>Page is accessible at `/identity/register`, uses shared layout, and allows identity creation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → API | User input crosses to backend. Server validates all fields per ID-06. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | IdentityForm | accept | Client-side form data can be tampered; server-side validation in `IdentityController.validateCreateRequest` is the source of truth (ID-06). |
| T-09-04 | Information Disclosure | register.tsx | mitigate | No secrets in page source. API errors are displayed inline (generic messages for 500s, detailed for 400s). |
</threat_model>

<verification>
- Page renders at `/identity/register` in Next.js dev mode.
- Form submission successfully calls `POST /identity/register`.
</verification>

<success_criteria>
- `/identity/register` displays a form with all required SigilIdentity fields.
- Submitting the form creates an identity and shows the resulting `identity_id`.
- Validation errors from the API appear inline.
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-ui-extensions/09-02-SUMMARY.md`
</output>
