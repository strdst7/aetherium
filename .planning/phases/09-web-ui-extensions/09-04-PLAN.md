---
phase: 09-web-ui-extensions
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - api/src/controllers/reason.ts
  - api/src/types/api-contracts.ts
  - web/components/ValidationReport.tsx
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "API ReasonResponse includes validationReport field"
    - "ValidationReport component renders pass/fail status, checks, and confidence score"
    - "No existing API contracts are broken (additive only)"
  artifacts:
    - path: "api/src/controllers/reason.ts"
      provides: "Updated ReasonResponse with validationReport"
    - path: "api/src/types/api-contracts.ts"
      provides: "ValidationReport re-export and contract update"
    - path: "web/components/ValidationReport.tsx"
      provides: "Validation report UI component"
      exports: ["ValidationReport"]
  key_links:
    - from: "api/src/controllers/reason.ts"
      to: "api/src/types/halo.ts"
      via: "import { ValidationReport }"
    - from: "web/components/ValidationReport.tsx"
      to: "web/src/types/api.ts"
      via: "import { ValidationReport }"
---

<objective>
Expose the Sovereign Halo validation report through the API and build a `ValidationReport` component to display it on generation results (UI-04).

Purpose: The validation report is the core output of the Sovereign Halo (Phase 7). Without it in the API response and UI, developers cannot verify identity law compliance.
Output: Updated API contracts and `ValidationReport` React component.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@api/src/controllers/reason.ts
@api/src/types/halo.ts
@api/src/services/orchestrator.ts
@web/src/types/api.ts
@design/sigil/v1.json
</context>

<tasks>

<task type="auto">
  <name>task 1: update API ReasonResponse to include validationReport</name>
  <files>api/src/controllers/reason.ts, api/src/types/api-contracts.ts</files>
  <read_first>
    - api/src/controllers/reason.ts (current ReasonResponse interface and response construction)
    - api/src/types/halo.ts (ValidationReport shape)
    - api/src/services/orchestrator.ts (OrchestratorResponse includes validationReport)
    - api/src/types/api-contracts.ts (ReasonResponse definition for downstream consumers)
  </read_first>
  <action>
    1. In `api/src/controllers/reason.ts`:
       - Add `import { ValidationReport } from "../types/halo";` (if not already present).
       - Update the local `ReasonResponse` interface to include `validationReport?: ValidationReport;`.
       - In the response object construction (after `orchestratorResponse` is available), add:
         ```typescript
         validationReport: orchestratorResponse.validationReport,
         ```
         Note: `orchestratorResponse` comes from `this.orchestrator.process()` which returns `OrchestratorResponse` containing `validationReport`. In the `agentBuilder` paths, `orchestratorResponse` is manually constructed and may lack `validationReport`; that's acceptable because the field is optional.
    2. In `api/src/types/api-contracts.ts`:
       - Update the `ReasonResponse` interface to include `validationReport?: ValidationReport;`.
       - Ensure `ValidationReport` is already re-exported from `./halo` (it is).
  </action>
  <acceptance_criteria>
    - `api/src/controllers/reason.ts` local `ReasonResponse` interface contains `validationReport?: ValidationReport`
    - `api/src/controllers/reason.ts` response object assigns `validationReport: orchestratorResponse.validationReport`
    - `api/src/types/api-contracts.ts` `ReasonResponse` interface contains `validationReport?: ValidationReport`
  </acceptance_criteria>
  <verify>
    <automated>
      COUNT1=$(grep -c "validationReport" api/src/controllers/reason.ts) && [ "$COUNT1" -ge 3 ] && \
      COUNT2=$(grep -c "validationReport" api/src/types/api-contracts.ts) && [ "$COUNT2" -ge 1 ]
    </automated>
  </verify>
  <done>API ReasonResponse now includes the optional `validationReport` field, populated from the orchestrator output.</done>
</task>

<task type="auto">
  <name>task 2: create ValidationReport UI component</name>
  <files>web/components/ValidationReport.tsx</files>
  <read_first>
    - web/src/types/api.ts (ValidationReport and ValidationCheck shapes)
    - design/sigil/v1.json (semantic colors)
    - web/pages/index.tsx (existing reflective result display for style reference)
  </read_first>
  <action>
    Create `web/components/ValidationReport.tsx`:
    - Props:
      ```typescript
      interface ValidationReportProps {
        report: ValidationReport;
      }
      ```
    - Render a container card with:
      - **Header**: "Sovereign Halo Validation Report" + status badge.
        - Badge text: `report.status.toUpperCase()`.
        - Badge color: green (`#27ae60` bg, white text) if `passed`, red (`#e74c3c` bg, white text) if `failed`.
      - **Summary bar**: `passedCount / totalCount` checks passed, `confidenceScore` displayed as percentage.
      - **Checks table** (or list):
        - Each check row shows: rule name (`check.rule.name`), pass/fail icon (✓ or ✗), detail text (`check.detail`), confidence percentage.
        - Failed checks get a left border in accent color (`#e74c3c`).
      - **Attempt info**: If `report.attemptNumber` is present, render "Validation attempt #{attemptNumber}".
    - If `report` is undefined or null, render nothing (or "No validation report available.").
    - Use tokens for typography and spacing.
  </action>
  <acceptance_criteria>
    - `web/components/ValidationReport.tsx` exports `ValidationReport`
    - Component renders `report.status` in a colored badge
    - Component maps over `report.checks` to display each check
    - Component displays `report.confidenceScore` as a percentage
    - Component conditionally displays `report.attemptNumber`
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "export function ValidationReport" web/components/ValidationReport.tsx && \
      grep -c "checks.map" web/components/ValidationReport.tsx && \
      grep -c "confidenceScore" web/components/ValidationReport.tsx && \
      grep -c "attemptNumber" web/components/ValidationReport.tsx
    </automated>
  </verify>
  <done>Component correctly renders the full validation report structure from the API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → UI | ValidationReport data is produced server-side and displayed client-side. No user input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Information Disclosure | ValidationReport component | accept | Report data belongs to the user who generated it. No leakage of other users' data. |
| T-09-09 | Tampering | validationReport field | accept | Field is read-only display. Server is source of truth. |
| T-09-10 | Denial of Service | large checks array | mitigate | Cap rendering to first 50 checks with a "+N more" indicator to prevent UI lock on pathological reports. |
</threat_model>

<verification>
- `npm test` in `api/` passes after controller changes.
- `cd web && npx tsc --noEmit` compiles with the new types.
- Component renders sample `ValidationReport` without errors.
</verification>

<success_criteria>
- `POST /v1/reason` returns `validationReport` when Sovereign Halo is active.
- `ValidationReport` component displays status, checks, confidence, and attempt number.
- Existing API consumers are not broken (field is optional).
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-ui-extensions/09-04-SUMMARY.md`
</output>
