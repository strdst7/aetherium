---
phase: 09-web-ui-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types/api.ts
  - web/src/lib/api-client.ts
  - web/components/IdentitySelector.tsx
  - web/components/Layout.tsx
  - web/src/tokens.ts
autonomous: true
requirements:
  - UI-06
must_haves:
  truths:
    - "A shared API client exists for all Phase 9 pages"
    - "An IdentitySelector dropdown is available and fetches identities from the API"
    - "A Layout component provides consistent navigation across pages"
  artifacts:
    - path: "web/src/types/api.ts"
      provides: "Shared TypeScript types for API responses"
      exports: ["SigilIdentity", "ValidationReport", "ReasoningResponse"]
    - path: "web/src/lib/api-client.ts"
      provides: "API fetch wrapper"
      exports: ["getIdentities", "registerIdentity", "getMemoryShards", "submitReasonRequest"]
    - path: "web/components/IdentitySelector.tsx"
      provides: "Identity dropdown component"
      exports: ["IdentitySelector"]
    - path: "web/components/Layout.tsx"
      provides: "Shared page layout with navigation"
      exports: ["Layout"]
    - path: "web/src/tokens.ts"
      provides: "Design token constants"
      exports: ["tokens"]
  key_links:
    - from: "web/components/IdentitySelector.tsx"
      to: "web/src/lib/api-client.ts"
      via: "import { getIdentities }"
      pattern: "getIdentities"
    - from: "web/components/Layout.tsx"
      to: "web/components/IdentitySelector.tsx"
      via: "import { IdentitySelector }"
      pattern: "IdentitySelector"
---

<objective>
Establish shared UI infrastructure for Phase 9: reusable types, API client, identity selector, layout shell, and design tokens.

Purpose: All subsequent Phase 9 pages depend on these primitives. Centralizing them prevents duplication and ensures consistent API contract alignment.
Output: Types, API client, IdentitySelector, Layout, tokens.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design/sigil/v1.json
</context>

<tasks>

<task type="auto">
  <name>task 1: create shared types and API client</name>
  <files>web/src/types/api.ts, web/src/lib/api-client.ts</files>
  <read_first>
    - api/src/types/api-contracts.ts (source of truth for API shapes)
    - api/src/types/identity.ts (identity shapes)
    - api/src/types/halo.ts (validation report shapes)
    - web/pages/index.tsx (to see current client-side types)
  </read_first>
  <action>
    1. Create `web/src/types/api.ts` with the following exports:
       - `SigilIdentity`, `IdentityVersion`, `IdentityCreateRequest`, `IdentityUpdateRequest` aligned with the API contracts.
       - `ValidationReport`, `ValidationCheck`, `ValidationRule` aligned with `api/src/types/halo.ts`.
       - `MemoryDocument` aligned with `api/src/services/memory-service.ts` `MemoryDocument`.
       - `ReasoningResponse` aligned with `api/src/controllers/reason.ts` `ReasonResponse`, but with `validationReport?: ValidationReport` added (forward-compatible for Plan 04).
       - `ReasoningTrace`, `OrchestratorReasoning`, `ReflectiveCheckResult`, etc. as needed.
    2. Create `web/src/lib/api-client.ts` with async functions:
       - `getIdentities(): Promise<SigilIdentity[]>` → `GET /identity`
       - `registerIdentity(payload: IdentityCreateRequest): Promise<SigilIdentity>` → `POST /identity/register`
       - `getMemoryShards(identityId?: string): Promise<MemoryDocument[]>` → `GET /v1/memory/all` (or `/v1/memory?identity_id=...` when Plan 05 adds it)
       - `submitReasonRequest(identity_anchor: string, messages: {role:string;content:string}[], options?: object): Promise<ReasoningResponse>` → `POST /v1/reason`
       Base URL: `process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8080'`.
       Include `Content-Type: application/json` headers and basic error handling (throw on !res.ok).
  </action>
  <acceptance_criteria>
    - `web/src/types/api.ts` exports `ReasoningResponse` containing `validationReport?: ValidationReport`
    - `web/src/types/api.ts` exports `SigilIdentity` with at least `id`, `name`, `sigil_hash`
    - `web/src/lib/api-client.ts` exports `getIdentities`, `registerIdentity`, `getMemoryShards`, `submitReasonRequest`
    - `web/src/lib/api-client.ts` uses `process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8080'` as base URL
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "export interface ReasoningResponse" web/src/types/api.ts && \
      grep -c "validationReport" web/src/types/api.ts && \
      grep -c "getIdentities" web/src/lib/api-client.ts && \
      grep -c "registerIdentity" web/src/lib/api-client.ts && \
      grep -c "getMemoryShards" web/src/lib/api-client.ts && \
      grep -c "submitReasonRequest" web/src/lib/api-client.ts && \
      grep -c "NEXT_PUBLIC_API_URL" web/src/lib/api-client.ts
    </automated>
  </verify>
  <done>Types and API client exist, align with backend contracts, and compile without TypeScript errors.</done>
</task>

<task type="auto">
  <name>task 2: create IdentitySelector and Layout components with design tokens</name>
  <files>web/src/tokens.ts, web/components/IdentitySelector.tsx, web/components/Layout.tsx</files>
  <read_first>
    - web/pages/index.tsx (existing inline style patterns)
    - design/sigil/v1.json (design tokens source of truth)
  </read_first>
  <action>
    1. Use existing `web/src/tokens.ts` exports (`colors`, `spacing`, `typography`, `geometry`, `shadows`, `motion`) in components. Do NOT remove or alter existing exports (`designTokens`, `geometry`, `colors`, `typography`, `spacing`, `shadows`, `motion`, `sigilComplianceRules`, `generateCSSVariables`, `applyCSSVariables`). If a convenience `tokens` aggregate object is helpful, add it as an additional named export without changing existing ones.
    2. Create `web/components/IdentitySelector.tsx`:
       - Props: `value: string; onChange: (value: string) => void;`
       - On mount, call `getIdentities()` from `web/src/lib/api-client.ts`, store in state.
       - Render `<select>` with options: first option "Select Identity...", then map identities to `<option value={identity.id}>{identity.name}</option>`.
       - Bind `value` and `onChange`.
       - Inline styles using tokens (padding 10px, border 1px solid neutral[300], borderRadius 4px, minWidth 200px).
    3. Create `web/components/Layout.tsx`:
       - Props: `children: React.ReactNode`
       - Render a full-width header (background white, bottom border 2px solid primary[500], padding 16px 24px).
       - Inside header: brand title "⚡ Aetherium" on the left, horizontal nav links in the center (Home, Register Identity, Memory, Agents, Governance using `<a href="/...">`), and `<IdentitySelector>` on the right.
       - Render `<main>` with maxWidth 1200px, margin auto, padding 20px, background neutral[50].
       - Wrap `children` inside main.
  </action>
  <acceptance_criteria>
    - `web/src/tokens.ts` retains existing exports (`colors`, `spacing`, `typography`, `geometry`, `shadows`, `motion`) and optionally adds `tokens`
    - `web/components/IdentitySelector.tsx` exports `IdentitySelector` function component
    - `web/components/IdentitySelector.tsx` imports `getIdentities` from `../src/lib/api-client`
    - `web/components/Layout.tsx` exports `Layout` function component
    - `web/components/Layout.tsx` imports `IdentitySelector` from `./IdentitySelector`
    - `web/components/Layout.tsx` contains nav links to `/`, `/identity/register`, `/memory`, `/agents`, `/governance`
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "export const colors" web/src/tokens.ts && \
      grep -c "export function IdentitySelector" web/components/IdentitySelector.tsx && \
      grep -c "getIdentities" web/components/IdentitySelector.tsx && \
      grep -c "export function Layout" web/components/Layout.tsx && \
      grep -c "IdentitySelector" web/components/Layout.tsx && \
      grep -c "/identity/register" web/components/Layout.tsx && \
      grep -c "/memory" web/components/Layout.tsx
    </automated>
  </verify>
  <done>Shared components render without runtime errors in Next.js dev mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | All API calls cross from browser (untrusted) to backend. Input must be validated server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | api-client.ts | mitigate | Base URL comes from env var; no secrets embedded in client bundle. |
| T-09-02 | Tampering | IdentitySelector dropdown | accept | Dropdown values are rendered from server data; tampering is client-side only and revalidated on server. |
</threat_model>

<verification>
- All files compile via `cd web && npx tsc --noEmit`
- `npm test` in `web/` passes existing tests (none yet, but must not break build)
</verification>

<success_criteria>
- `web/src/types/api.ts`, `web/src/lib/api-client.ts`, `web/src/tokens.ts`, `web/components/IdentitySelector.tsx`, `web/components/Layout.tsx` exist and are exported.
- `IdentitySelector` fetches identities from `GET /identity` on mount.
- `Layout` wraps pages with navigation and the identity selector.
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-ui-extensions/09-01-SUMMARY.md`
</output>
