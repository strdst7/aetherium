---
phase: 09-web-ui-extensions
plan: 07
type: execute
wave: 4
depends_on:
  - 09-02
  - 09-03
  - 09-04
  - 09-05
  - 09-06
files_modified:
  - web/pages/_app.tsx
  - web/src/lib/identity-context.tsx
  - web/components/Layout.tsx
  - web/pages/index.test.tsx
  - web/pages/identity/register.test.tsx
  - web/pages/memory.test.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
must_haves:
  truths:
    - "All Phase 9 pages are linked in the shared navigation"
    - "Active identity is persisted via React Context across page navigations"
    - "Jest tests exist and pass for key pages and components"
  artifacts:
    - path: "web/pages/_app.tsx"
      provides: "Global app wrapper with identity context"
    - path: "web/src/lib/identity-context.tsx"
      provides: "React context for active identity"
      exports: ["IdentityProvider", "useIdentity"]
    - path: "web/components/Layout.tsx"
      provides: "Updated layout with navigation links and global identity selector"
    - path: "web/pages/index.test.tsx"
      provides: "Tests for Identity Test page"
    - path: "web/pages/identity/register.test.tsx"
      provides: "Tests for registration page"
    - path: "web/pages/memory.test.tsx"
      provides: "Tests for memory inspection page"
  key_links:
    - from: "web/pages/_app.tsx"
      to: "web/src/lib/identity-context.tsx"
      via: "import { IdentityProvider }"
    - from: "web/components/Layout.tsx"
      to: "web/src/lib/identity-context.tsx"
      via: "import { useIdentity }"
---

<objective>
Wire all Phase 9 pages together with global identity persistence, shared navigation, and Jest tests.

Purpose: Without global state, the active identity resets on every page navigation, breaking the user journey. Tests prove the UI works end-to-end.
Output: `_app.tsx`, `identity-context.tsx`, updated `Layout.tsx`, and test files.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@web/components/Layout.tsx
@web/jest.config.js
@web/jest.setup.js
</context>

<tasks>

<task type="auto">
  <name>task 1: create global identity context and update layout</name>
  <files>web/src/lib/identity-context.tsx, web/components/Layout.tsx, web/pages/_app.tsx</files>
  <read_first>
    - web/components/Layout.tsx (current state from Plan 01)
    - web/pages/_app.tsx (may not exist)
    - web/components/IdentitySelector.tsx
    - web/jest.config.js (to confirm moduleNameMapper for @/ alias)
  </read_first>
  <action>
    1. Create `web/src/lib/identity-context.tsx`:
       - Define `IdentityContext` with `{ activeIdentity: string | null; setActiveIdentity: (id: string | null) => void; }`.
       - `IdentityProvider` component using `useState` to hold `activeIdentity`.
       - `useIdentity()` hook that returns context value.
       - Default `activeIdentity` can be `null`.
    2. Update `web/components/Layout.tsx`:
       - Import `useIdentity` from `../src/lib/identity-context`.
       - Replace the local `IdentitySelector` state with the global one: `<IdentitySelector value={activeIdentity || ''} onChange={(id) => setActiveIdentity(id || null)} />`.
       - Ensure nav links are present for all pages: `/`, `/identity/register`, `/memory`, `/agents`, `/governance`.
    3. Create `web/pages/_app.tsx`:
       - Import `IdentityProvider` from `../src/lib/identity-context`.
       - Import `Layout` from `../components/Layout`.
       - Export default function `MyApp({ Component, pageProps })` that returns:
         ```tsx
         <IdentityProvider>
           <Layout>
             <Component {...pageProps} />
           </Layout>
         </IdentityProvider>
         ```
       - Add `import type { AppProps } from 'next/app';` for TypeScript.
    4. Update `web/pages/index.tsx` (minor sync from Plan 06 if not already done):
       - Import `useIdentity` and sync `identityAnchor` state with `activeIdentity` on mount and when dropdown changes.
       - This ensures the global selector and page state are bi-directionally synced.
  </action>
  <acceptance_criteria>
    - `web/src/lib/identity-context.tsx` exports `IdentityProvider` and `useIdentity`
    - `web/components/Layout.tsx` imports `useIdentity` from `../src/lib/identity-context`
    - `web/components/Layout.tsx` renders `<IdentitySelector>` bound to `activeIdentity`
    - `web/pages/_app.tsx` exports default component wrapping `IdentityProvider` and `Layout`
    - `web/pages/index.tsx` imports `useIdentity` and syncs local state with global context
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "export function IdentityProvider" web/src/lib/identity-context.tsx && \
      grep -c "export function useIdentity" web/src/lib/identity-context.tsx && \
      grep -c "useIdentity" web/components/Layout.tsx && \
      grep -c "IdentityProvider" web/pages/_app.tsx && \
      grep -c "Layout" web/pages/_app.tsx && \
      grep -c "useIdentity" web/pages/index.tsx
    </automated>
  </verify>
  <done>Global identity context is active, Layout uses it, and all pages share the same selected identity.</done>
</task>

<task type="auto">
  <name>task 2: add Jest tests for Phase 9 pages</name>
  <files>web/pages/index.test.tsx, web/pages/identity/register.test.tsx, web/pages/memory.test.tsx</files>
  <read_first>
    - web/jest.config.js
    - web/jest.setup.js
    - web/src/lib/api-client.ts (to understand functions to mock)
    - web/pages/index.tsx
    - web/pages/identity/register.tsx
    - web/pages/memory.tsx
  </read_first>
  <action>
    Create three test files using `@testing-library/react` and `jest` mocks:

    1. `web/pages/index.test.tsx`:
       - Mock `web/src/lib/api-client` (jest.mock).
       - Render the Home page.
       - Assert that `IdentitySelector` is present (query for `<select>` or mock component).
       - Simulate selecting an identity and entering a prompt.
       - Simulate form submit.
       - Assert `submitReasonRequest` is called with correct arguments.
       - Mock a response with `validationReport` and `reasoning.trace`, then assert `ValidationReport` and `ReasoningTrace` render (check for text contained in mock data).

    2. `web/pages/identity/register.test.tsx`:
       - Mock `registerIdentity`.
       - Render register page.
       - Fill form fields (name, developerId, etc.).
       - Click submit.
       - Assert `registerIdentity` called with payload matching form values.
       - Assert success message appears with `identity_id`.

    3. `web/pages/memory.test.tsx`:
       - Mock `getMemoryShards`.
       - Render memory page.
       - Assert `getMemoryShards` called without identity initially (or with null).
       - Simulate identity selection.
       - Assert `getMemoryShards` called with the selected identity ID.
       - Assert filtered shard content appears.

    Use `waitFor` from `@testing-library/react` for async assertions.
  </action>
  <acceptance_criteria>
    - `web/pages/index.test.tsx` exists and tests identity selection, prompt submission, and response rendering
    - `web/pages/identity/register.test.tsx` exists and tests form submission and success state
    - `web/pages/memory.test.tsx` exists and tests identity filter and re-fetch
    - All three files import `render` from `@testing-library/react`
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "import { render" web/pages/index.test.tsx && \
      grep -c "import { render" web/pages/identity/register.test.tsx && \
      grep -c "import { render" web/pages/memory.test.tsx && \
      cd web && npm test -- --testPathPattern="pages/(index|identity/register|memory)\.test" --passWithNoTests
    </automated>
  </verify>
  <done>Tests pass and verify the critical user journeys of Phase 9.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React Context → localStorage | If context is persisted to localStorage, untrusted browser data crosses into app state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | Tampering | localStorage identity | mitigate | Do NOT persist `activeIdentity` to `localStorage` without validation. For v1, keep context in-memory only (no localStorage) to avoid XSS/data-tampering risks. |
| T-09-16 | Information Disclosure | test mocks | accept | Tests use mocked data; no real secrets or PII. |
</threat_model>

<verification>
- `cd web && npm test` passes (including new tests).
- `cd web && npx tsc --noEmit` compiles.
- Manual check: selecting an identity on `/`, navigating to `/memory`, and seeing the same identity pre-selected in the header dropdown.
</verification>

<success_criteria>
- `_app.tsx` wraps every page with `IdentityProvider` and `Layout`.
- `Layout` uses global `useIdentity` so the dropdown is synchronized across all pages.
- Jest tests exist for index, register, and memory pages and pass.
- No regressions in existing pages (`/agents`, `/governance`).
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-ui-extensions/09-07-SUMMARY.md`
</output>
