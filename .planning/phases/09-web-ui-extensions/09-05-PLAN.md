---
phase: 09-web-ui-extensions
plan: 05
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - api/src/controllers/memory.ts
  - web/pages/memory.tsx
  - web/src/lib/api-client.ts
autonomous: true
requirements:
  - UI-05
must_haves:
  truths:
    - "API supports fetching memory shards scoped to an identity"
    - "Memory inspection page has an identity dropdown to filter shards"
    - "Visualization updates to show only filtered shards (or highlight them)"
  artifacts:
    - path: "api/src/controllers/memory.ts"
      provides: "Identity-scoped memory endpoint"
    - path: "web/pages/memory.tsx"
      provides: "Enhanced memory inspection page"
    - path: "web/src/lib/api-client.ts"
      provides: "Updated getMemoryShards with identity param"
  key_links:
    - from: "web/pages/memory.tsx"
      to: "web/src/lib/api-client.ts"
      via: "getMemoryShards(identityId)"
    - from: "api/src/controllers/memory.ts"
      to: "api/src/services/memory-service.ts"
      via: "memoryService.searchByMetadata"
---

<objective>
Enhance the Memory Inspection page so developers can filter memory shards to the selected identity (UI-05), and add the supporting backend endpoint.

Purpose: Memory retrieval must be strictly identity-scoped (critical constraint from Phase 5). The UI must enforce this visually.
Output: Updated `memory.ts` controller and refactored `memory.tsx` page.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@api/src/controllers/memory.ts
@api/src/services/memory-service.ts
@web/pages/memory.tsx
@web/src/lib/api-client.ts
@web/components/IdentitySelector.tsx
</context>

<tasks>

<task type="auto">
  <name>task 1: add identity-scoped memory endpoint</name>
  <files>api/src/controllers/memory.ts</files>
  <read_first>
    - api/src/controllers/memory.ts (existing routes)
    - api/src/services/memory-service.ts (searchByMetadata method)
  </read_first>
  <action>
    In `api/src/controllers/memory.ts`:
    - Add a new route `router.get("/v1/memory", async (req, res) => { ... })`.
    - Read `req.query.identity_id` as optional string.
    - If `identity_id` is provided:
      - Call `memoryService.searchByMetadata({ sigil: identity_id }, 1000)` to return all shards tagged with that identity.
      - Return JSON array of matching documents.
    - If `identity_id` is not provided:
      - Call `memoryService.getAll()` and return all documents (same behavior as `/v1/memory/all` for backward compatibility).
    - Leave `/v1/memory/all` untouched to preserve existing clients.
  </action>
  <acceptance_criteria>
    - `api/src/controllers/memory.ts` contains `router.get("/v1/memory", ...)`
    - Handler reads `req.query.identity_id`
    - When `identity_id` is present, calls `memoryService.searchByMetadata({ sigil: identity_id }, ...)`
    - When absent, falls back to `memoryService.getAll()`
    - `/v1/memory/all` route remains unchanged
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c 'router.get("/v1/memory"' api/src/controllers/memory.ts && \
      grep -c "identity_id" api/src/controllers/memory.ts && \
      grep -c "searchByMetadata" api/src/controllers/memory.ts && \
      grep -c 'router.get("/v1/memory/all"' api/src/controllers/memory.ts
    </automated>
  </verify>
  <done>New endpoint returns identity-scoped memory shards without breaking the existing `/v1/memory/all` route.</done>
</task>

<task type="auto">
  <name>task 2: refactor memory inspection page with identity filter</name>
  <files>web/pages/memory.tsx, web/src/lib/api-client.ts</files>
  <read_first>
    - web/pages/memory.tsx (current implementation using UMAP and trace)
    - web/components/IdentitySelector.tsx
    - web/src/lib/api-client.ts
    - web/src/types/api.ts
  </read_first>
  <action>
    1. Update `web/src/lib/api-client.ts`:
       - Modify `getMemoryShards(identityId?: string): Promise<MemoryDocument[]>` so that if `identityId` is provided, it calls `/v1/memory?identity_id=${encodeURIComponent(identityId)}`; otherwise it calls `/v1/memory/all`.
    2. Refactor `web/pages/memory.tsx`:
       - Import `IdentitySelector` from `../components/IdentitySelector`.
       - Add state `selectedIdentity: string | null`.
       - Add a section above the visualization with label "Filter by Identity:" and `<IdentitySelector value={selectedIdentity || ''} onChange={(id) => setSelectedIdentity(id)} />`.
       - In the `useEffect` that loads data, call `getMemoryShards(selectedIdentity || undefined)` instead of the hardcoded `/v1/memory/all` fetch.
       - When `selectedIdentity` changes, re-fetch shards and re-run UMAP on the filtered set.
       - In the SVG visualization, ensure circles are rendered only for the filtered shards (since the data is already filtered).
       - In the Trace section, keep the existing trace query but optionally pass `identity_id` if the backend trace endpoint supports it (it does not yet; leave a TODO comment or skip).
       - Update the page heading to "Memory Inspection".
  </action>
  <acceptance_criteria>
    - `web/src/lib/api-client.ts` `getMemoryShards` accepts optional `identityId` and calls `/v1/memory?identity_id=...` when provided
    - `web/pages/memory.tsx` imports `IdentitySelector`
    - `web/pages/memory.tsx` has state for `selectedIdentity`
    - `web/pages/memory.tsx` fetches shards via `getMemoryShards(selectedIdentity || undefined)` inside `useEffect`
    - UMAP is re-run when selected identity changes
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "getMemoryShards" web/src/lib/api-client.ts && \
      grep -c "identityId" web/src/lib/api-client.ts && \
      grep -c "IdentitySelector" web/pages/memory.tsx && \
      grep -c "selectedIdentity" web/pages/memory.tsx && \
      grep -c "getMemoryShards" web/pages/memory.tsx
    </automated>
  </verify>
  <done>Memory page shows shards filtered by the selected identity, and the filter triggers a re-fetch and re-visualization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API memory endpoint | `identity_id` query param crosses from browser to backend. Must be validated as string, not executed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering | identity_id query param | accept | MongoDB driver parameterizes queries; `searchByMetadata` uses object literal filter, not raw strings. No injection risk. |
| T-09-12 | Information Disclosure | memory shards | mitigate | Endpoint filters by `sigil` field. A user can only see shards tagged with the identity they know. This is best-effort scoping; true isolation is server-side in MemoryService. |
</threat_model>

<verification>
- `GET /v1/memory?identity_id=sigil:v1:halo-arc:001` returns only shards with `sigil` matching that value.
- `GET /v1/memory/all` continues to return all shards.
- Memory page re-filters and re-renders UMAP when identity changes.
</verification>

<success_criteria>
- Memory inspection page has an `IdentitySelector` dropdown.
- Selecting an identity fetches and displays only shards scoped to that identity.
- Existing `/v1/memory/all` behavior is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-ui-extensions/09-05-SUMMARY.md`
</output>
