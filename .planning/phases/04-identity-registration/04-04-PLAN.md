---
phase: 04-identity-registration
plan: 04
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
files_modified:
  - api/src/types/api-contracts.ts
  - api/openapi.yml
  - api/src/index.ts
  - api/src/tests/identity-integration.test.ts
autonomous: true
requirements:
  - ID-01
  - ID-02
  - ID-03
  - ID-04
  - ID-05
  - ID-06
must_haves:
  truths:
    - "api-contracts.ts exports SigilIdentity and identity-related types for downstream consumers"
    - "openapi.yml documents all five identity endpoints with request/response schemas"
    - "Identity routes are registered in the Express application bootstrap"
    - "Integration tests exercise the full identity lifecycle: register → get → list → update → versions"
  artifacts:
    - path: "api/src/types/api-contracts.ts"
      provides: "Identity-related types exported alongside existing API contract types"
      contains: ["SigilIdentity", "IdentityVersion", "IdentityCreateRequest", "IdentityUpdateRequest"]
    - path: "api/openapi.yml"
      provides: "OpenAPI documentation for all identity endpoints"
      contains: ["/identity/register", "/identity/{id}", "/identity/{id}/versions"]
    - path: "api/src/index.ts"
      provides: "Bootstrap wiring for IdentityService and identity routes"
      contains: ["new IdentityService", "createIdentityRouter"]
    - path: "api/src/tests/identity-integration.test.ts"
      provides: "End-to-end integration tests"
      min_lines: 60
  key_links:
    - from: "api/src/index.ts"
      to: "api/src/controllers/identity.ts"
      via: "app.use(createIdentityRouter(service))"
      pattern: "createIdentityRouter"
    - from: "api/openapi.yml"
      to: "api/src/types/api-contracts.ts"
      via: "Schema definitions aligned with TypeScript types"
      pattern: "SigilIdentity|IdentityVersion"
---

<objective>
Finalize the identity API by updating contracts, documentation, bootstrap wiring, and running integration tests.

Purpose: Ensures the identity endpoints are discoverable, documented, and fully wired into the running application. Integration tests verify the complete lifecycle end-to-end.
Output: Updated api-contracts.ts, openapi.yml, index.ts bootstrap, and passing integration tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-identity-registration/04-CONTEXT.md
@api/src/types/api-contracts.ts
@api/openapi.yml
@api/src/index.ts
@api/src/services/identity-service.ts
@api/src/controllers/identity.ts
</context>

<interfaces>
<!-- Existing patterns to follow -->

From api/src/types/api-contracts.ts:
```typescript
export type ApiVersion = "1.0.0";
export const CURRENT_API_VERSION: ApiVersion = "1.0.0";
export interface VersionedResponse { apiVersion: ApiVersion; }
export interface ProblemDetails {
  type: string;
  title: string;
  status: number;
  detail: string;
  instance?: string;
  errors?: Array<{ field: string; message: string; code: string }>;
  apiVersion: ApiVersion;
}
```

From api/openapi.yml (existing pattern):
```yaml
paths:
  /v1/reason:
    post:
      summary: Natural language reasoning
      operationId: reason
      tags: [Reasoning]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
      responses:
        '200':
          description: Reasoning response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReasonResponse'
```

From api/src/index.ts (existing bootstrap pattern):
```typescript
const memoryService = new MemoryService();
await memoryService.connect(process.env.MONGODB_URI);
app.use("/", createMemoryRouter(memoryService));
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: add identity types to api-contracts.ts</name>
  <files>api/src/types/api-contracts.ts</files>
  <read_first>
    - api/src/types/api-contracts.ts (existing types)
    - api/src/types/identity.ts (source of truth for identity types)
  </read_first>
  <action>
    Modify api/src/types/api-contracts.ts to re-export identity types for downstream consumers.

    Add the following re-exports at the bottom of the file:
    ```typescript
    // Identity types (Phase 4)
    export {
      SigilIdentity,
      SigilIdentitySnapshot,
      IdentityVersion,
      IdentityCreateRequest,
      IdentityUpdateRequest,
    } from "./identity";
    ```

    Do NOT duplicate the interface definitions — use re-exports to maintain a single source of truth.
    Ensure the file still compiles and all existing exports remain intact.
  </action>
  <verify>
    <automated>cd api && npx tsc --noEmit src/types/api-contracts.ts</automated>
  </verify>
  <done>api-contracts.ts re-exports all 5 identity types, no duplicate definitions, compilation passes</done>
</task>

<task type="auto">
  <name>task 2: document identity endpoints in openapi.yml</name>
  <files>api/openapi.yml</files>
  <read_first>
    - api/openapi.yml (existing OpenAPI structure and schema patterns)
    - api/src/types/identity.ts (field definitions)
  </read_first>
  <action>
    Modify api/openapi.yml to add identity endpoints and schemas.

    Add under `paths:` (before or after existing paths):

    ```yaml
    /identity/register:
      post:
        summary: Register a new SigilIdentity
        operationId: registerIdentity
        tags:
          - Identity
        requestBody:
          required: true
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityCreateRequest'
        responses:
          '201':
            description: Identity created
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/SigilIdentity'
          '400':
            description: Validation failed
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ProblemDetails'

    /identity/{id}:
      get:
        summary: Get identity by ID
        operationId: getIdentityById
        tags:
          - Identity
        parameters:
          - name: id
            in: path
            required: true
            schema:
              type: string
        responses:
          '200':
            description: Identity found
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/SigilIdentity'
          '404':
            description: Identity not found
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ProblemDetails'
      put:
        summary: Update an existing identity
        operationId: updateIdentity
        tags:
          - Identity
        parameters:
          - name: id
            in: path
            required: true
            schema:
              type: string
        requestBody:
          required: true
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityUpdateRequest'
        responses:
          '200':
            description: Identity updated
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/SigilIdentity'
          '400':
            description: Validation failed
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ProblemDetails'
          '404':
            description: Identity not found
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ProblemDetails'

    /identity:
      get:
        summary: List all identities
        operationId: listIdentities
        tags:
          - Identity
        responses:
          '200':
            description: List of identities
            content:
              application/json:
                schema:
                  type: object
                  properties:
                    identities:
                      type: array
                      items:
                        $ref: '#/components/schemas/SigilIdentity'
                    count:
                      type: integer

    /identity/{id}/versions:
      get:
        summary: Get version history for an identity
        operationId: getIdentityVersions
        tags:
          - Identity
        parameters:
          - name: id
            in: path
            required: true
            schema:
              type: string
        responses:
          '200':
            description: Version history
            content:
              application/json:
                schema:
                  type: object
                  properties:
                    identity_id:
                      type: string
                    versions:
                      type: array
                      items:
                        $ref: '#/components/schemas/IdentityVersion'
                    count:
                      type: integer
          '404':
            description: Identity not found
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ProblemDetails'
    ```

    Add under `components.schemas:`:

    ```yaml
    SigilIdentity:
      type: object
      required:
        - identity_id
        - name
        - voice
        - sigil_hash
        - version
        - created_at
        - updated_at
        - versions
      properties:
        identity_id:
          type: string
          example: "id_aB3x9KpLmQ"
        name:
          type: string
          maxLength: 128
        voice:
          type: string
          maxLength: 256
        constraints:
          type: array
          items:
            type: string
            maxLength: 512
        mythic_signature:
          type: string
          maxLength: 1024
        allowed_behaviors:
          type: array
          items:
            type: string
            maxLength: 256
        forbidden_behaviors:
          type: array
          items:
            type: string
            maxLength: 256
        sigil_hash:
          type: string
          pattern: '^[a-f0-9]{64}$'
        version:
          type: integer
          minimum: 1
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        versions:
          type: array
          items:
            $ref: '#/components/schemas/IdentityVersion'

    SigilIdentitySnapshot:
      type: object
      required:
        - name
        - voice
      properties:
        name:
          type: string
          maxLength: 128
        voice:
          type: string
          maxLength: 256
        constraints:
          type: array
          items:
            type: string
            maxLength: 512
        mythic_signature:
          type: string
          maxLength: 1024
        allowed_behaviors:
          type: array
          items:
            type: string
            maxLength: 256
        forbidden_behaviors:
          type: array
          items:
            type: string
            maxLength: 256

    IdentityVersion:
      type: object
      required:
        - version_number
        - sigil_hash
        - updated_at
        - snapshot
      properties:
        version_number:
          type: integer
          minimum: 1
        sigil_hash:
          type: string
          pattern: '^[a-f0-9]{64}$'
        updated_at:
          type: string
          format: date-time
        snapshot:
          $ref: '#/components/schemas/SigilIdentitySnapshot'

    IdentityCreateRequest:
      type: object
      required:
        - name
        - voice
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[a-zA-Z0-9\\s\\-]+$'
        voice:
          type: string
          minLength: 1
          maxLength: 256
        constraints:
          type: array
          maxItems: 50
          items:
            type: string
            minLength: 1
            maxLength: 512
        mythic_signature:
          type: string
          maxLength: 1024
        allowed_behaviors:
          type: array
          maxItems: 100
          items:
            type: string
            minLength: 1
            maxLength: 256
        forbidden_behaviors:
          type: array
          maxItems: 100
          items:
            type: string
            minLength: 1
            maxLength: 256

    IdentityUpdateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[a-zA-Z0-9\\s\\-]+$'
        voice:
          type: string
          minLength: 1
          maxLength: 256
        constraints:
          type: array
          maxItems: 50
          items:
            type: string
            minLength: 1
            maxLength: 512
        mythic_signature:
          type: string
          maxLength: 1024
        allowed_behaviors:
          type: array
          maxItems: 100
          items:
            type: string
            minLength: 1
            maxLength: 256
        forbidden_behaviors:
          type: array
          maxItems: 100
          items:
            type: string
            minLength: 1
            maxLength: 256
    ```

    IMPORTANT: The `pattern` fields in YAML must escape backslashes correctly. Use double backslashes in the YAML string (e.g., `pattern: '^[a-zA-Z0-9\\s\\-]+$'`).

    Also update the `ApiInfoResponse.capabilities` example to include `"identity"` if not already present.
  </action>
  <verify>
    <automated>cd api && npx swagger-cli validate openapi.yml 2>/dev/null || echo "swagger-cli not installed, skipping validation"</automated>
  </verify>
  <done>openapi.yml contains all 4 identity paths, 5 identity schemas, and validates structurally</done>
</task>

<task type="auto">
  <name>task 3: wire identity service and routes into bootstrap</name>
  <files>api/src/index.ts</files>
  <read_first>
    - api/src/index.ts (current bootstrap code)
    - api/src/services/identity-service.ts (service to instantiate)
    - api/src/controllers/identity.ts (router factory)
  </read_first>
  <action>
    Modify api/src/index.ts to bootstrap the IdentityService and register identity routes.

    Add after the MemoryService initialization:
    ```typescript
    // Initialize identity service
    const identityService = new IdentityService();
    await identityService.connect(process.env.MONGODB_URI);
    console.log("✅ Identity service connected");
    ```

    Add after the documentation routes registration:
    ```typescript
    // Register identity routes
    import { createIdentityRouter } from "./controllers/identity";
    app.use("/", createIdentityRouter(identityService));
    console.log("✅ Identity routes registered");
    ```

    Add identityService to the health check info if the health router supports dynamic checks. If the health router's createHealthRouter accepts an object, add `identityService` to the argument object passed to `createHealthRouter`. If it does not, skip this step.

    Ensure the import for `createIdentityRouter` is at the top of the file alongside other imports.

    The file must still compile and all existing functionality must remain intact.
  </action>
  <verify>
    <automated>cd api && npx tsc --noEmit src/index.ts</automated>
  </verify>
  <done>index.ts instantiates IdentityService, connects to MongoDB, registers identity routes, compiles without errors</done>
</task>

<task type="auto" tdd="true">
  <name>task 4: integration tests for full identity lifecycle</name>
  <files>api/src/tests/identity-integration.test.ts</files>
  <read_first>
    - api/src/index.ts (to understand bootstrap for supertest)
    - api/src/controllers/identity.ts (endpoints to test)
    - api/src/services/identity-service.ts (service behavior)
    - api/src/types/identity.ts (response shapes)
  </read_first>
  <behavior>
    - Test 1: Full lifecycle — register → get by ID → list → update → get versions
    - Test 2: Registration validation rejects oversized arrays and strings
    - Test 3: Version history accumulates correctly across multiple updates
    - Test 4: sigil_hash changes after update and is deterministic on re-registration with same fields
    - Test 5: 404 responses return RFC 7807 Problem Details for unknown IDs on get, update, and versions
    - Test 6: List identities returns correct count and sorts by created_at descending
  </behavior>
  <action>
    Create api/src/tests/identity-integration.test.ts.

    Use supertest with the Express app instance. Import the `app` from `../index` if it's exported, OR construct a minimal Express app that mirrors the bootstrap logic (instantiate IdentityService, connect, mount router).

    If `app` is not exported from `index.ts`, create the test app like this:
    ```typescript
    import express from "express";
    import { IdentityService } from "../services/identity-service";
    import { createIdentityRouter } from "../controllers/identity";

    const app = express();
    app.use(express.json());
    const identityService = new IdentityService();
    beforeAll(async () => {
      await identityService.connect(process.env.MONGODB_URI || "mongodb://localhost:27017/aetherium_test");
      app.use("/", createIdentityRouter(identityService));
    });
    afterAll(async () => {
      await identityService.disconnect();
    });
    ```

    Test 1 (Full lifecycle):
    1. POST /identity/register with a valid body. Assert 201, response.body.identity_id starts with "id_", version === 1.
    2. GET /identity/{id} with the returned ID. Assert 200, body matches registered fields.
    3. GET /identity. Assert 200, body.count >= 1, body.identities is array.
    4. PUT /identity/{id} with `{ name: "Updated Name" }`. Assert 200, body.version === 2, body.name === "Updated Name".
    5. GET /identity/{id}/versions. Assert 200, body.count === 1, body.versions[0].version_number === 1, body.versions[0].snapshot.name is the original name.

    Test 2 (Validation):
    - POST /identity/register with name = "a".repeat(129) → 400, error field === "name".
    - POST /identity/register with constraints = Array(51).fill("x") → 400, error field === "constraints".

    Test 3 (Multiple versions):
    - Register identity.
    - Update name 3 times.
    - GET /identity/{id}/versions → count === 3, versions sorted by version_number ascending.

    Test 4 (Hash behavior):
    - Register two identities with identical fields → sigil_hashes are equal.
    - Update one identity → its sigil_hash changes, the other remains the same.

    Test 5 (404 Problem Details):
    - GET /identity/nonexistent → 404, body.type exists, body.title exists.
    - PUT /identity/nonexistent → 404, same structure.
    - GET /identity/nonexistent/versions → 404, same structure.

    Test 6 (List sorting):
    - Register 3 identities with different names.
    - GET /identity → assert identities[0].created_at >= identities[1].created_at >= identities[2].created_at.
  </action>
  <verify>
    <automated>cd api && npx jest src/tests/identity-integration.test.ts --coverage=false</automated>
  </verify>
  <done>All 6 integration tests pass, exercising the full identity lifecycle end-to-end</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenAPI spec → Client | Documented API contract crosses to untrusted consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Spoofing | OpenAPI spec does not match implementation | mitigate | OpenAPI schema field types, lengths, and patterns match the validation rules in IdentityController exactly |
| T-04-18 | Repudiation | no audit of bootstrap events | accept | Bootstrap logs to console; full audit is Phase 8 |
| T-04-19 | Information Disclosure | health endpoint exposes identity service status | accept | Health checks return only "ok/error" status, no data |
| T-04-20 | Denial of Service | integration tests against real MongoDB | accept | Tests use separate test database and clean up after |
</threat_model>

<verification>
- `cd api && npx tsc --noEmit` passes with no errors across the entire project
- `cd api && npx jest src/tests/identity-integration.test.ts` passes all tests
- `curl http://localhost:8080/identity` returns `{ "identities": [], "count": 0 }` when server is running with empty DB
</verification>

<success_criteria>
- api-contracts.ts re-exports all identity types without duplication
- openapi.yml documents all 5 identity endpoints with correct schemas and validation constraints
- index.ts bootstraps IdentityService and registers routes without breaking existing endpoints
- Integration tests pass covering full identity lifecycle
</success_criteria>

<output>
After completion, create `.planning/phases/04-identity-registration/04-04-SUMMARY.md`
</output>
