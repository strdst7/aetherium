---
phase: 05-identity-bound-reasoning
plan: 05
type: execute
wave: 3
depends_on:
  - 05-04
files_modified:
  - api/src/services/multi-agent-orchestrator.ts
  - api/src/services/multi-agent-orchestrator.test.ts
  - api/src/controllers/multi-agent.ts
  - api/src/controllers/multi-agent.test.ts
autonomous: true
requirements:
  - RE-03
  - RE-05
must_haves:
  truths:
    - "Multi-agent council outputs all align with the same active identity"
    - "Each agent in the council receives the same identity context"
    - "Council output reflects identity constraints"
  artifacts:
    - path: "api/src/services/multi-agent-orchestrator.ts"
      provides: "MultiAgentOrchestrator with identity binding"
      contains: ["identity", "identityBinding"]
    - path: "api/src/controllers/multi-agent.ts"
      provides: "Multi-agent routes pass identity"
      contains: ["identity_anchor"]
  key_links:
    - from: "MultiAgentOrchestrator"
      to: "IdentityBindingService"
      via: "constructor injection"
      pattern: "IdentityBindingService"
---

<objective>
Integrate identity binding into the Multi-Agent Orchestrator so that all council agents use the same active identity.

Purpose: The multi-agent council (Archivist, SigilKeeper, Narrator) must produce outputs aligned with the active identity. This plan ensures identity context is shared across all agents.
Output: MultiAgentOrchestrator with identity-aware council execution.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@api/src/services/multi-agent-orchestrator.ts
@api/src/controllers/multi-agent.ts
@api/src/services/identity-binding.ts
</context>

<tasks>
1. Update `MultiAgentOrchestrator` constructor:
   - Add `identityBinding?: IdentityBindingService` parameter
   - Store as private field

2. Update `MultiAgentOrchestrator.orchestrate()` to:
   - Load identity via `identityBinding.resolve(identity_anchor)` if available
   - Pass identity to each agent's reasoning call
   - Include identity context in the council output

3. Update `MultiAgentCouncilRequest` to include `identity_anchor: string`

4. Update `MultiAgentResponse` to include `identity` field (optional)

5. Update `createMultiAgentRouter()` to pass identity_binding to MultiAgentOrchestrator

6. Update `POST /multi-agent/reason` endpoint to:
   - Accept identity_anchor in request body
   - Pass identity to orchestrator
   - Include identity in response

7. Create tests:
   - Test: Multi-agent council loads identity before orchestration
   - Test: All agents receive same identity context
   - Test: Council output includes identity alignment
</tasks>

<summary_template>
```markdown
## Plan 05-05 Complete: Multi-Agent Identity Alignment

**Status**: ✅ COMPLETE

**What changed**:
- MultiAgentOrchestrator loads identity via IdentityBindingService
- Identity passed to each council agent
- Council output includes identity context
- Routes updated to pass identity

**Key decisions**:
- Single identity shared across all council agents
- Identity loading is optional (backward compatible)
- Council output includes identity for verification

**Tests passing**:
- Multi-agent identity tests: 3/3

**Next plan**: 05-06
```
</summary_template>
