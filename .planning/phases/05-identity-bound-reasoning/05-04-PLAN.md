---
phase: 05-identity-bound-reasoning
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - api/src/services/orchestrator.ts
  - api/src/services/orchestrator.test.ts
  - api/src/services/agent-builder.ts
  - api/src/services/agent-builder.test.ts
autonomous: true
requirements:
  - RE-01
  - RE-02
  - RE-05
  - RE-06
must_haves:
  truths:
    - "Orchestrator loads the active identity before executing any reasoning loop"
    - "Identity constraints are applied at every reasoning step"
    - "Identity-bound reasoning trace shows which rules were applied"
    - "Multi-agent council outputs align with the same active identity"
  artifacts:
    - path: "api/src/services/orchestrator.ts"
      provides: "Orchestrator with identity loading and constraint enforcement"
      contains: ["identityBinding", "loadIdentity", "applyConstraints"]
    - path: "api/src/services/agent-builder.ts"
      provides: "AgentBuilder with identity-aware execution"
      contains: ["identityBinding"]
  key_links:
    - from: "Orchestrator"
      to: "IdentityBindingService"
      via: "constructor injection"
      pattern: "IdentityBindingService"
    - from: "Orchestrator"
      to: "IdentityConstraintEngine"
      via: "evaluate constraints after each step"
      pattern: "IdentityConstraintEngine"
---

<objective>
Integrate identity binding into the Orchestrator and AgentBuilder so that every reasoning loop loads the active identity and enforces constraints.

Purpose: The core of Phase 5. This plan wires identity binding into the existing reasoning pipeline so that identity is loaded, constraints applied, and trace produced.
Output: Updated Orchestrator and AgentBuilder with identity-aware reasoning.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@api/src/services/orchestrator.ts
@api/src/services/agent-builder.ts
@api/src/services/identity-binding.ts
@api/src/services/identity-constraints.ts
@api/src/controllers/reason.ts
</context>

<tasks>
1. Update `Orchestrator` constructor:
   - Add `identityBinding?: IdentityBindingService` parameter
   - Store as private field

2. Update `Orchestrator.process()` to:
   - Load identity via `identityBinding.resolve(req.identity_anchor)` if available
   - If identity loaded, add identity context to prompt:
     ```
     Identity: {identity.name}
     Rules: {identity.config.customRules?.join(', ')}
     ```
   - Pass identity to ReflectiveService.evaluate()
   - Include identity in OrchestratorContext

3. Update `Orchestrator.processWithTools()` similarly:
   - Load identity
   - Add identity context to prompt
   - Pass identity to reflective evaluation

4. Update `AgentBuilder` constructor:
   - Add `identityBinding?: IdentityBindingService` parameter
   - Pass identity to orchestrator calls

5. Update `ReasonResponse` to include `identity` field (optional)

6. Update `ReasonController.handleReason()` to:
   - Pass identity info to response if available
   - Include identity loading in trace

7. Create tests:
   - Test: Orchestrator loads identity before reasoning
   - Test: Identity constraints are applied to output
   - Test: Identity context is included in reasoning trace
   - Test: AgentBuilder passes identity through to orchestrator
</tasks>

<summary_template>
```markdown
## Plan 05-04 Complete: Orchestrator Identity Integration

**Status**: ✅ COMPLETE

**What changed**:
- Orchestrator loads identity via IdentityBindingService
- Identity context injected into prompts
- ReflectiveService evaluates identity constraints
- ReasonResponse includes identity field
- AgentBuilder passes identity through

**Key decisions**:
- Identity loading is optional (backward compatible)
- Identity context prepended to system prompt
- Trace includes identity loading stage

**Tests passing**:
- Orchestrator identity tests: 4/4
- AgentBuilder identity tests: 2/2

**Next plan**: 05-05
```
</summary_template>
