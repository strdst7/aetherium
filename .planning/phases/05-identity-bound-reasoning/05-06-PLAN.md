---
phase: 05-identity-bound-reasoning
plan: 06
type: tdd
wave: 3
depends_on:
  - 05-03
  - 05-04
  - 05-05
files_modified:
  - api/src/tests/identity-bound-integration.test.ts
  - api/src/tests/identity-performance.test.ts
  - api/src/index.ts
autonomous: true
requirements:
  - RE-01
  - RE-02
  - RE-03
  - RE-04
  - RE-05
  - RE-06
must_haves:
  truths:
    - "Integration test proves full identity-bound pipeline: register → reason → verify identity in trace"
    - "Performance test verifies identity overhead ≤200ms"
    - "No cross-identity memory leakage in concurrent requests"
    - "API endpoints updated to support identity-bound reasoning"
  artifacts:
    - path: "api/src/tests/identity-bound-integration.test.ts"
      provides: "End-to-end integration tests for identity-bound reasoning"
      min_lines: 80
    - path: "api/src/tests/identity-performance.test.ts"
      provides: "Performance tests for identity overhead"
      min_lines: 40
  key_links:
    - from: "api/src/index.ts"
      to: "IdentityBindingService"
      via: "bootstrap wiring"
      pattern: "IdentityBindingService"
---

<objective>
Create integration and performance tests that validate the complete identity-bound reasoning pipeline. Wire all services in the Express bootstrap.

Purpose: Phase 5 integration test proves the full pipeline works. Performance test proves the 200ms latency constraint. Bootstrap wiring connects all services.
Output: Integration tests, performance tests, updated bootstrap.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@api/src/index.ts
@api/src/services/identity-binding.ts
@api/src/services/identity-constraints.ts
@api/src/services/orchestrator.ts
@api/src/services/multi-agent-orchestrator.ts
</context>

<tasks>
1. Update `api/src/index.ts` bootstrap:
   - Create `IdentityBindingService` after `IdentityService`
   - Pass `IdentityBindingService` to `Orchestrator` constructor
   - Pass `IdentityBindingService` to `AgentBuilder` constructor
   - Pass `IdentityBindingService` to `MultiAgentOrchestrator` constructor
   - Pass `IdentityBindingService` to `MultiAgentRouter` constructor
   - Update `createMultiAgentRouter` signature to accept identityBinding

2. Create `api/src/tests/identity-bound-integration.test.ts`:
   - Test: Register identity → submit reason request → verify identity loaded in trace
   - Test: Submit reason with identity → verify identity constraints applied in trace
   - Test: Submit reason with identity → verify memory is scoped (only identity memories)
   - Test: Two concurrent requests with different identities → no cross-identity leakage
   - Test: Multi-agent council with identity → verify identity in council output
   - Test: Identity not found → verify proper error (404 with Problem Details)

3. Create `api/src/tests/identity-performance.test.ts`:
   - Test: Measure identity lookup latency (must be <200ms)
   - Test: Measure total request latency with identity (must be <5s)
   - Test: Cached identity lookup is <10ms
   - Test: Uncached identity lookup is <200ms
</tasks>

<summary_template>
```markdown
## Plan 05-06 Complete: Integration & Performance Tests

**Status**: ✅ COMPLETE

**What changed**:
- Bootstrap wired IdentityBindingService to all orchestrators
- 6 integration tests covering full pipeline
- 4 performance tests verifying latency constraints
- All services connected end-to-end

**Key decisions**:
- Identity binding injected into all orchestrators via constructor
- Integration tests use mock providers and mock MongoDB
- Performance tests measure identity lookup overhead

**Tests passing**:
- Integration tests: 6/6
- Performance tests: 4/4

**Phase 5 complete!**
```
</summary_template>
