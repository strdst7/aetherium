---
phase: 03-agent-interface-contracts
plan: 04
type: tdd
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - api/src/middleware/openapi-validator.ts
  - api/src/tests/contract.test.ts
  - api/src/tests/backward-compat.test.ts
  - api/src/tests/version-negotiation.test.ts
autonomous: true
requirements:
  - AG-08
must_haves:
  truths:
    - "All API responses validate against OpenAPI schema"
    - "Existing web UI requests continue to work unchanged"
    - "Version negotiation rejects unsupported versions"
    - "Error responses match ProblemDetails schema"
  artifacts:
    - path: "api/src/tests/contract.test.ts"
      provides: "Contract validation tests"
      contains: "describe('API Contract'"
    - path: "api/src/tests/backward-compat.test.ts"
      provides: "Backward compatibility tests"
      contains: "describe('Backward Compatibility'"
  key_links:
    - from: "api/src/tests/contract.test.ts"
      to: "api/openapi.yml"
      via: "schema validation"
      pattern: "openapi schema"
---

<objective>
Validate that all API responses conform to the published OpenAPI contract and that existing integrations remain backward-compatible. Add runtime schema validation and comprehensive contract tests.

Purpose: A contract is only as good as its enforcement. Tests ensure that code changes don't accidentally break the API contract or backward compatibility.
Output: Contract validation middleware, contract tests, backward compatibility tests, version negotiation tests
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-agent-interface-contracts/03-CONTEXT.md
@api/openapi.yml (from Plan 03-01)
@api/src/controllers/reason.ts
@api/src/middleware/version-negotiation.ts (from Plan 03-02)
@api/src/middleware/error-handler.ts (from Plan 03-02)
@api/src/index.ts (from Plan 03-03)

<interfaces>
From api/src/controllers/reason.ts:
```typescript
export interface ReasonRequest {
  identity_anchor: string;
  messages: Array<{ role: string; content: string }>;
  options?: { ... };
}

export interface ReasonResponse {
  id: string;
  output: string;
  status: "approved" | "refine" | "reject";
  identity_anchor: string;
  reasoning: { ... };
  toolCalls?: ToolCall[];
  toolResults?: ToolExecutionResult[];
  toolExecutionTrace?: ToolExecutionTrace[];
  plan?: TaskPlan;
  actions?: Action[];
  planStatus?: PlanStatus;
  metadata: { processingTimeMs: number; version: string; apiVersion: string; mode: "tool" | "task"; };
}
```

From api/src/types/api-contracts.ts:
```typescript
export interface ProblemDetails {
  type: string;
  title: string;
  status: number;
  detail: string;
  instance?: string;
  errors?: Array<{ field: string; message: string }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Create contract validation middleware</name>
  <files>api/src/middleware/openapi-validator.ts</files>
  <behavior>
    - Test 1: Valid ReasonRequest passes validation (identity_anchor and messages present)
    - Test 2: ReasonRequest missing identity_anchor fails with 400 and validation errors array
    - Test 3: ReasonRequest with empty messages array fails with 400
    - Test 4: ReasonRequest with invalid temperature (>2) fails with 400
    - Test 5: ReasonRequest with invalid mode (not "tool" or "task") fails with 400
    - Test 6: Valid ReasonResponse passes validation (all required fields present)
    - Test 7: ReasonResponse missing apiVersion fails validation
    - Test 8: ProblemDetails response validates against schema (type, title, status, detail present)
  </behavior>
  <action>
Create `api/src/middleware/openapi-validator.ts` that validates requests and responses against the OpenAPI spec:

1. Install `ajv` and `ajv-formats` if not present:
   ```json
   "ajv": "^8.12.0",
   "ajv-formats": "^2.1.1"
   ```
2. Load `api/openapi.yml` and extract schemas for:
   - `ReasonRequest`
   - `ReasonResponse`
   - `ProblemDetails`
3. Create `validateRequest(schemaName, data)` function that:
   - Uses AJV to compile the schema
   - Returns `{ valid: true }` or `{ valid: false, errors: [...] }`
4. Create `requestValidator` middleware that:
   - Runs on POST /v1/reason (can be applied selectively)
   - Validates `req.body` against `ReasonRequest` schema
   - On failure: calls `next()` with a ValidationError that the error handler will format as ProblemDetails
5. Create `responseValidator` middleware (for development/testing only) that:
   - Validates outgoing JSON responses against `ReasonResponse` schema
   - Logs validation errors to console but does NOT block the response (to avoid breaking production)
   - Should be toggleable via environment variable `VALIDATE_RESPONSES=true`
6. Export `createValidator(openapiPath)` factory function

Use the actual OpenAPI schemas loaded from `api/openapi.yml` — do NOT duplicate schema definitions in TypeScript. This ensures the validator always matches the published spec.
  </action>
  <verify>
    <automated>test -f api/src/middleware/openapi-validator.ts && grep -q "ajv" api/src/middleware/openapi-validator.ts && grep -q "ReasonRequest" api/src/middleware/openapi-validator.ts && grep -q "validateRequest" api/src/middleware/openapi-validator.ts</automated>
  </verify>
  <done>Request validator exists, uses OpenAPI schemas via AJV, validates ReasonRequest and ReasonResponse</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Write contract and backward compatibility tests</name>
  <files>api/src/tests/contract.test.ts, api/src/tests/backward-compat.test.ts, api/src/tests/version-negotiation.test.ts</files>
  <behavior>
    - Test 1: POST /v1/reason returns 200 with valid ReasonResponse (all required fields, apiVersion present)
    - Test 2: POST /v1/reason with invalid body returns 400 with ProblemDetails format
    - Test 3: POST /v1/reason with unsupported version header returns 406
    - Test 4: GET /health returns 200 with status, timestamp, version
    - Test 5: GET /v1/info returns 200 with apiVersion, supportedVersions, features
    - Test 6: GET /docs returns 200 HTML
    - Test 7: GET /openapi.yml returns valid YAML
    - Test 8: Web UI request format (from web/pages/index.tsx) still works unchanged
    - Test 9: ReasonResponse from Phase 2 (with plan, actions, planStatus) validates against v1 schema
    - Test 10: ReasonResponse without optional tool fields still validates
    - Test 11: Version negotiation accepts v1 via header, query param, and path
    - Test 12: Version negotiation rejects v99 with 406
  </behavior>
  <action>
Create three test files:

**api/src/tests/contract.test.ts:**
1. Import `supertest`, `express`, and the app setup from `../index` or create a test app
2. Test `POST /v1/reason`:
   - Valid request returns 200 and response body contains: id, output, status, identity_anchor, reasoning, metadata.apiVersion
   - Response metadata.apiVersion equals "1.0.0"
   - Response contains reasoning.trace array
3. Test `GET /health`:
   - Returns 200, body has status, timestamp, version fields
4. Test `GET /v1/info`:
   - Returns 200, body has apiVersion, supportedVersions, features, documentationUrl
5. Test `GET /docs`:
   - Returns 200 with Content-Type text/html
6. Test `GET /openapi.yml`:
   - Returns 200 with Content-Type text/yaml
   - Body contains `openapi: 3.0.3`
7. Test error formatting:
   - POST /v1/reason with missing identity_anchor returns 400
   - Response body has `type`, `title`, `status`, `detail` (ProblemDetails format)

**api/src/tests/backward-compat.test.ts:**
1. Test that the exact request format used by `web/pages/index.tsx` still works:
   ```json
   {
     "identity_anchor": "sigil:v1:halo-arc:001",
     "messages": [{"role":"user","content":"Test prompt"}],
     "options": {"maxTokens":512,"temperature":0.7,"memoryK":5}
   }
   ```
2. Test that the response format expected by the web UI is still returned:
   - response.id exists
   - response.output exists
   - response.status is one of approved/refine/reject
   - response.reasoning.orchestrator.selectedProvider exists
   - response.reasoning.reflective.status exists
   - response.reasoning.trace is array
3. Test that optional Phase 2 fields (plan, actions, planStatus) are present when task mode is used
4. Test that the old `metadata.version` field still exists alongside new `metadata.apiVersion`

**api/src/tests/version-negotiation.test.ts:**
1. Test version detection:
   - `Accept-Version: v1` → request processed with version 1.0.0
   - `Accept-Version: 1.0.0` → request processed
   - Query param `?api-version=v1` → request processed
   - Path `/v1/reason` → request processed
2. Test version rejection:
   - `Accept-Version: v2` → 406 with ProblemDetails
   - `Accept-Version: 99.0.0` → 406 with ProblemDetails
   - Response detail mentions supported versions
3. Test default behavior:
   - No version specified → request processed (defaults to current)

Use `supertest` for HTTP testing. Mock the Orchestrator/AgentBuilder for unit tests. For integration tests, use the existing mock infrastructure (`MockAgentBuilder`, `MockMCPServer`).
  </action>
  <verify>
    <automated>test -f api/src/tests/contract.test.ts && test -f api/src/tests/backward-compat.test.ts && test -f api/src/tests/version-negotiation.test.ts && grep -q "describe('API Contract'" api/src/tests/contract.test.ts && grep -q "describe('Backward Compatibility'" api/src/tests/backward-compat.test.ts && grep -q "describe('Version Negotiation'" api/src/tests/version-negotiation.test.ts</automated>
  </verify>
  <done>All three test files exist, cover contract validation, backward compatibility, and version negotiation</done>
</task>

<task type="auto">
  <name>task 3: Wire request validator into app</name>
  <files>api/src/index.ts</files>
  <action>
Update `api/src/index.ts` to use the request validator:

1. Import `createValidator` from `./middleware/openapi-validator`
2. After creating the validator, apply it to the POST /v1/reason route:
   ```typescript
   const { requestValidator } = createValidator(path.join(__dirname, "../openapi.yml"));
   
   app.post("/v1/reason", requestValidator, async (req, res, next) => {
     // existing handler
   });
   ```
3. The validator should run AFTER body parsing (`express.json()`) but BEFORE the route handler
4. Ensure the validator error is passed to `next(error)` so the error handler formats it as ProblemDetails

Important: Do NOT apply response validation in production unless `VALIDATE_RESPONSES=true` is set. Response validation can be added to test setup only.
  </action>
  <verify>
    <automated>grep -q "requestValidator" api/src/index.ts && grep -q "openapi-validator" api/src/index.ts</automated>
  </verify>
  <done>Request validator is applied to POST /v1/reason, errors flow to error handler</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → API | Contract tests validate API behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Denial of Service | Schema validation overhead | accept | AJV compiles schemas once; validation is fast |
| T-03-12 | Information Disclosure | Test fixtures expose internal types | accept | Test types mirror public API; no secrets |
| T-03-13 | Repudiation | Missing contract tests | mitigate | Tests cover all public endpoints and response formats |
</threat_model>

<verification>
- [ ] `npm test` passes with all contract tests
- [ ] `npm test` passes with all backward compatibility tests
- [ ] `npm test` passes with all version negotiation tests
- [ ] Invalid requests return 400 with ProblemDetails
- [ ] Unsupported versions return 406 with ProblemDetails
- [ ] Valid requests return 200 with apiVersion in metadata
- [ ] Web UI request format works unchanged
</verification>

<success_criteria>
- All API responses validate against OpenAPI schema
- Existing web UI integration continues to work
- Version negotiation rejects unsupported versions
- Error responses follow ProblemDetails format
- Contract tests provide regression protection
- Backward compatibility is verified by automated tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-interface-contracts/03-04-SUMMARY.md`
</output>
